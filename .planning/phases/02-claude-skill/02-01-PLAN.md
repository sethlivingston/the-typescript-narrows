---
phase: 02-claude-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skill/the-typescript-narrows/SKILL.md
autonomous: true
requirements: [SKIL-18, SKIL-19, SKIL-20, SKIL-21, SKIL-10]

must_haves:
  truths:
    - "SKILL.md exists with valid Agent Skills frontmatter"
    - "SKILL.md has a 10-20 line preamble explaining the skill, severity tiers, and exception clause"
    - "SKILL.md lists all 50 opinions grouped by topic with one-sentence stances"
    - "SKILL.md is under 500 lines total"
    - "Each topic group links to its reference file"
  artifacts:
    - path: "skill/the-typescript-narrows/SKILL.md"
      provides: "Skill index with frontmatter, preamble, and opinion listing"
      contains: "name: the-typescript-narrows"
  key_links:
    - from: "skill/the-typescript-narrows/SKILL.md"
      to: "skill/the-typescript-narrows/references/*.md"
      via: "relative markdown links at end of each topic section"
      pattern: "references/.*\\.md"
---

<objective>
Create the SKILL.md index file for The TypeScript Narrows Claude skill.

Purpose: SKILL.md is the entry point an AI agent loads when activated. It must provide the full opinion index (all 50 opinions with one-sentence stances) plus a preamble explaining how to use the skill. This file serves as the table of contents; detailed content lives in reference files created by subsequent plans.

Output: `skill/the-typescript-narrows/SKILL.md` -- a valid Agent Skills spec file under 500 lines.
</objective>

<execution_context>
@/Users/sethlivingston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sethlivingston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-claude-skill/02-CONTEXT.md
@.planning/phases/02-claude-skill/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SKILL.md with frontmatter, preamble, and full 50-opinion index</name>
  <files>skill/the-typescript-narrows/SKILL.md</files>
  <read_first>
    - docs/opinions/INDEX.md (source of truth for all 50 opinions, topic groups, severity/enforcement tags)
    - docs/opinions/README.md (corpus philosophy, severity tiers, exception clause -- informs preamble)
    - .planning/phases/02-claude-skill/02-RESEARCH.md (Agent Skills spec requirements, frontmatter constraints, example formats)
    - .planning/phases/02-claude-skill/02-CONTEXT.md (locked decisions on structure, preamble, format)
  </read_first>
  <action>
Create directory `skill/the-typescript-narrows/references/` (mkdir -p).

Create `skill/the-typescript-narrows/SKILL.md` with these exact sections:

**1. YAML frontmatter:**
```yaml
---
name: the-typescript-narrows
description: Provides a single, well-reasoned TypeScript opinion for every common decision point. Eliminates the "multiple ways to do it" problem when writing, refactoring, or reviewing TypeScript code. Use when generating TypeScript, performing TypeScript code review, or deciding between multiple valid TypeScript patterns.
---
```
- `name` MUST be `the-typescript-narrows` (38 chars, valid per spec)
- `description` MUST be third-person, include what the skill does AND when to use it, max 1024 chars

**2. Title and tagline:**
```
# The TypeScript Narrows

One opinion per decision point. When TypeScript gives you five valid ways, this skill picks one and tells you why.
```

**3. Preamble (10-20 lines):**
Must include:
- What this skill is (single sentence)
- How to apply opinions (when writing, refactoring, reviewing TypeScript)
- Severity tiers table: Bug prevention [B], Maintenance [M], Style [S]
- Global exception clause (allow exceptions when no viable alternative; convenience is not a valid exception; document why in a code comment)
- Note that each opinion has a stance, rationale, and do/don't examples in the reference files

**4. Opinion index (12 topic groups, 50 opinions total):**
Each topic group is an H2 heading. Each opinion is a bullet with:
- Bold one-sentence stance (derived from the opinion's Stance section, rewritten in imperative voice)
- Severity tag in brackets: [B], [M], or [S]

After all bullets in a topic group, add a link line:
`For rationale and examples: [references/{filename}.md](references/{filename}.md)`

Topic groups and their opinions (use INDEX.md as the canonical list -- transform each opinion's title into an imperative one-sentence stance):

**Type Safety** (7 opinions) -> references/type-safety.md
- no-explicit-any, prefer-unknown, no-type-assertions, no-non-null-assertion, strict-boolean-expressions, use-unknown-in-catch, no-unsafe-return

**Type Declarations** (5 opinions) -> references/type-declarations.md
- prefer-interface, ban-enums, no-namespace, consistent-type-imports, no-const-enum

**Null Handling and Narrowing** (6 opinions) -> references/null-handling.md
- exhaustive-switch, prefer-optional-chaining, no-unnecessary-condition, prefer-nullish-coalescing, use-type-narrowing, strict-null-checks

**Async and Promises** (5 opinions) -> references/async-promises.md
- no-floating-promises, no-misused-promises, require-await, return-await, prefer-async-await

**Error Handling** (4 opinions) -> references/error-handling.md
- typed-errors, result-over-throw, error-discrimination, no-empty-catch

**Immutability and Const** (5 opinions) -> references/immutability.md
- prefer-const, no-var, prefer-readonly, prefer-readonly-params, no-mutable-exports

**Module Organization** (4 opinions) -> references/modules.md
- named-exports-only, ban-barrel-files, explicit-imports, no-circular-deps

**Naming Conventions** (3 opinions) -> references/naming.md
- naming-convention, no-hungarian-notation, boolean-naming

**Discriminated Unions** (3 opinions) -> references/discriminated-unions.md
- single-discriminant, no-destructure-before-narrow, exhaustive-discrimination

**Generics and Advanced Types** (5 opinions -- includes branded-types from "Branded and Nominal Types" group and variance-annotations from "Generics and Advanced Types") -> references/generics.md
- constrain-generics, no-unnecessary-generics, prefer-generics-over-overloads, variance-annotations, branded-types

**tsconfig and Advanced Pitfalls** (3 opinions -- merges "tsconfig Strictness" and "Conditional Types and Advanced Pitfalls") -> references/tsconfig-advanced.md
- strict-tsconfig, no-unchecked-index, conditional-type-safety

Total: 50 opinions across 11 reference files (merging small groups per research recommendation).

**Content rules:**
- All opinions treated equally in the index -- no special section for bug-prevention tier
- Severity tags [B]/[M]/[S] appear inline per opinion (not as a separate highlight)
- Imperative voice for stances ("Never use explicit `any`" not "Explicit any should not be used")
- No framework-specific content (no React, Next, Express, Angular, Vue mentions)
- SKILL.md must be under 500 lines total -- if approaching 400 lines, tighten preamble
  </action>
  <verify>
    <automated>wc -l skill/the-typescript-narrows/SKILL.md | awk '{if ($1 < 500) print "PASS: "$1" lines"; else print "FAIL: "$1" lines (must be under 500)"}'</automated>
  </verify>
  <acceptance_criteria>
    - skill/the-typescript-narrows/SKILL.md exists
    - File starts with `---` (YAML frontmatter delimiter)
    - File contains `name: the-typescript-narrows`
    - File contains `description:` with non-empty value
    - File contains `# The TypeScript Narrows` heading
    - File contains exactly 11 H2 topic group headings (## Type Safety, ## Type Declarations, ## Null Handling, ## Async, ## Error Handling, ## Immutability, ## Module Organization, ## Naming Conventions, ## Discriminated Unions, ## Generics, ## tsconfig)
    - File contains exactly 50 opinion bullet points (lines starting with `- **`)
    - File contains 11 reference file links (one per topic group, pattern: `references/*.md`)
    - File contains severity tags [B], [M], or [S] on every opinion bullet
    - File contains "Bug prevention" or "[B]" in preamble severity explanation
    - File contains "exception" in preamble (exception clause)
    - File is under 500 lines (wc -l)
    - Directory skill/the-typescript-narrows/references/ exists
    - No occurrence of "react", "next", "express", "angular", "vue" (case-insensitive)
  </acceptance_criteria>
  <done>SKILL.md exists with valid frontmatter, 10-20 line preamble, and all 50 opinions indexed across 11 topic groups with severity tags and reference file links. File is under 500 lines.</done>
</task>

</tasks>

<verification>
- `wc -l skill/the-typescript-narrows/SKILL.md` shows under 500 lines
- `grep -c '^\- \*\*' skill/the-typescript-narrows/SKILL.md` returns 50
- `grep -c 'references/.*\.md' skill/the-typescript-narrows/SKILL.md` returns at least 11
- `grep -ic 'react\|next\|express\|angular\|vue' skill/the-typescript-narrows/SKILL.md` returns 0
- `head -5 skill/the-typescript-narrows/SKILL.md` shows YAML frontmatter with name field
</verification>

<success_criteria>
SKILL.md is a valid Agent Skills spec file serving as the index for The TypeScript Narrows skill. All 50 opinions appear in the index with severity tags. The file is under 500 lines. Reference file links point to the correct filenames. No framework-specific content.
</success_criteria>

<output>
After completion, create `.planning/phases/02-claude-skill/02-01-SUMMARY.md`
</output>

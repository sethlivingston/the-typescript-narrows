---
phase: 02-claude-skill
plan: 05
type: execute
wave: 3
depends_on: [02-02, 02-03, 02-04]
files_modified:
  - skill/the-typescript-narrows/SKILL.md
autonomous: true
requirements: [SKIL-19, SKIL-20, SKIL-21, SKIL-10]

must_haves:
  truths:
    - "All 50 opinions from docs/opinions/INDEX.md appear in SKILL.md index"
    - "All 50 opinions appear across the 11 reference files with Stance/Why/Do/Don't sections"
    - "SKILL.md is under 500 lines"
    - "No framework-specific content appears anywhere in skill files"
    - "Every opinion has a Why rationale in its reference file"
    - "SKILL.md reference links resolve to existing files"
  artifacts:
    - path: "skill/the-typescript-narrows/SKILL.md"
      provides: "Validated skill index"
      contains: "name: the-typescript-narrows"
  key_links:
    - from: "skill/the-typescript-narrows/SKILL.md"
      to: "skill/the-typescript-narrows/references/*.md"
      via: "all reference links resolve to existing files"
      pattern: "references/.*\\.md"
---

<objective>
Validate the complete Claude skill for coverage, format compliance, and quality.

Purpose: Ensure all 50 opinions made it through the transformation, SKILL.md meets the 500-line budget, no framework-specific content leaked in, every opinion has a Why rationale, and all reference file links resolve. Fix any issues found.

Output: A validated, complete Claude skill ready for Phase 5 publishing.
</objective>

<execution_context>
@/Users/sethlivingston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sethlivingston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-claude-skill/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate coverage, format, and fix any issues</name>
  <files>skill/the-typescript-narrows/SKILL.md</files>
  <read_first>
    - docs/opinions/INDEX.md (canonical list of all 50 opinions -- source of truth for coverage check)
    - skill/the-typescript-narrows/SKILL.md (the index to validate)
    - skill/the-typescript-narrows/references/type-safety.md
    - skill/the-typescript-narrows/references/type-declarations.md
    - skill/the-typescript-narrows/references/null-handling.md
    - skill/the-typescript-narrows/references/async-promises.md
    - skill/the-typescript-narrows/references/error-handling.md
    - skill/the-typescript-narrows/references/immutability.md
    - skill/the-typescript-narrows/references/modules.md
    - skill/the-typescript-narrows/references/naming.md
    - skill/the-typescript-narrows/references/discriminated-unions.md
    - skill/the-typescript-narrows/references/generics.md
    - skill/the-typescript-narrows/references/tsconfig-advanced.md
  </read_first>
  <action>
Run the following validation checks and fix any issues found:

**Check 1: Opinion count in SKILL.md index**
Count lines matching `^- \*\*` in SKILL.md. Must equal exactly 50.
If not 50, cross-reference against docs/opinions/INDEX.md to find missing or duplicate opinions and fix SKILL.md.

**Check 2: Opinion count in reference files**
Count H2 headings (lines matching `^## `) across all 11 reference files. Must total exactly 50.
Expected per file: type-safety(7), type-declarations(5), null-handling(6), async-promises(5), error-handling(4), immutability(5), modules(4), naming(3), discriminated-unions(3), generics(5), tsconfig-advanced(3).
If any file has wrong count, cross-reference against INDEX.md and fix.

**Check 3: Why rationale coverage**
Count occurrences of `**Why:**` across all reference files. Must equal exactly 50.
If any opinion is missing a Why section, add it based on the corresponding docs/opinions/*.md file.

**Check 4: SKILL.md line count**
Run `wc -l skill/the-typescript-narrows/SKILL.md`. Must be under 500.
If over 500, tighten preamble or stance descriptions.

**Check 5: No framework references**
Run `grep -ri 'react\|next\.js\|nextjs\|express\|angular\|vue\|svelte' skill/the-typescript-narrows/` (case-insensitive).
Must return no matches. If any found, remove the framework-specific content.

**Check 6: Reference file links resolve**
For each link in SKILL.md matching `references/*.md`, verify the target file exists.
If any link is broken, fix the filename in SKILL.md or create the missing file.

**Check 7: Do/Don't code examples**
Count occurrences of ` ```typescript` in each reference file. Each opinion should have at least 2 code blocks (Do + Don't).
Minimum total across all files: 100 (50 opinions x 2 blocks each).
If any opinion is missing code examples, add them from the corpus.

**Check 8: Frontmatter validity**
Verify SKILL.md starts with `---`, contains `name: the-typescript-narrows`, contains `description:` with non-empty value, and ends the frontmatter block with `---`.

If ALL checks pass with no fixes needed, document the validation results but make no changes to SKILL.md. Only modify files if issues are found.
  </action>
  <verify>
    <automated>echo "=== Check 1: SKILL.md opinion count ===" && grep -c '^\- \*\*' skill/the-typescript-narrows/SKILL.md && echo "=== Check 2: Reference file opinion count ===" && grep -c '^\#\# ' skill/the-typescript-narrows/references/*.md && echo "=== Check 3: Why rationale count ===" && grep -rc '\*\*Why:\*\*' skill/the-typescript-narrows/references/*.md && echo "=== Check 4: Line count ===" && wc -l skill/the-typescript-narrows/SKILL.md && echo "=== Check 5: Framework references ===" && grep -ric 'react\|next\.js\|nextjs\|express\|angular\|vue\|svelte' skill/the-typescript-narrows/ || echo "No framework references found (PASS)" && echo "=== Check 6: Reference links ===" && grep -o 'references/[a-z-]*\.md' skill/the-typescript-narrows/SKILL.md | sort -u | while read f; do test -f "skill/the-typescript-narrows/$f" && echo "EXISTS: $f" || echo "MISSING: $f"; done</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contains exactly 50 opinion bullets (lines matching `^- \*\*`)
    - Total H2 headings across all 11 reference files equals 50
    - Total `**Why:**` occurrences across all reference files equals 50
    - SKILL.md is under 500 lines
    - `grep -ric 'react\|next\.js\|nextjs\|express\|angular\|vue\|svelte' skill/the-typescript-narrows/` returns 0 or only returns 0-count lines
    - All reference file links in SKILL.md resolve to existing files
    - Total typescript code blocks across all reference files is at least 100
    - SKILL.md frontmatter contains `name: the-typescript-narrows` and non-empty `description:`
  </acceptance_criteria>
  <done>All 50 opinions verified present in both SKILL.md index and reference files. SKILL.md under 500 lines. No framework references. All links resolve. Every opinion has Why rationale and Do/Don't examples.</done>
</task>

</tasks>

<verification>
All 8 validation checks pass. The skill is complete and ready for Phase 5 publishing.
</verification>

<success_criteria>
The complete Claude skill passes all validation checks: 50/50 opinion coverage, under 500 lines, no framework content, all links resolve, every opinion has Why/Do/Don't sections.
</success_criteria>

<output>
After completion, create `.planning/phases/02-claude-skill/02-05-SUMMARY.md`
</output>

---
phase: 01-opinion-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - docs/opinions/exhaustive-switch.md
  - docs/opinions/prefer-optional-chaining.md
  - docs/opinions/no-unnecessary-condition.md
  - docs/opinions/prefer-nullish-coalescing.md
  - docs/opinions/use-type-narrowing.md
  - docs/opinions/strict-null-checks.md
  - docs/opinions/no-floating-promises.md
  - docs/opinions/no-misused-promises.md
  - docs/opinions/require-await.md
  - docs/opinions/return-await.md
  - docs/opinions/prefer-async-await.md
  - docs/opinions/typed-errors.md
  - docs/opinions/result-over-throw.md
  - docs/opinions/error-discrimination.md
  - docs/opinions/no-empty-catch.md
  - docs/opinions/conditional-type-safety.md
autonomous: true
requirements: [OPIN-02, OPIN-03]

must_haves:
  truths:
    - "16 opinion files exist covering Null Handling, Async, Error Handling, and Conditional Types topics"
    - "Every opinion file has valid YAML frontmatter with all required fields"
    - "Every opinion file has Stance, Why, Do, and Don't body sections with TypeScript code examples"
    - "Skill-only opinions do not have a lint field; lint-enforceable opinions include the lint mapping"
  artifacts:
    - path: "docs/opinions/no-floating-promises.md"
      provides: "Critical async bug prevention opinion"
      contains: "severity: bug-prevention"
    - path: "docs/opinions/result-over-throw.md"
      provides: "Differentiator error handling opinion"
      contains: "enforcement: skill-only"
    - path: "docs/opinions/conditional-type-safety.md"
      provides: "Advanced type pitfall opinion"
      contains: "enforcement: skill-only"
  key_links:
    - from: "docs/opinions/typed-errors.md"
      to: "docs/opinions/use-unknown-in-catch.md"
      via: "related field cross-reference"
      pattern: "related:.*use-unknown-in-catch"
---

<objective>
Author 16 opinion files covering Null Handling & Narrowing (6), Async & Promises (5), Error Handling (4), and Conditional Types (1).

Purpose: Creates the second batch of opinions focusing on runtime safety -- the patterns most likely to prevent bugs in production code.
Output: 16 markdown files in docs/opinions/
</objective>

<execution_context>
@/Users/sethlivingston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sethlivingston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-opinion-foundation/01-CONTEXT.md
@.planning/phases/01-opinion-foundation/01-RESEARCH.md
@.planning/phases/01-opinion-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Opinion file template. Every file MUST follow this exact structure. -->

```yaml
---
id: slug-based-id                   # Required. Matches filename (without .md)
title: Human-readable title          # Required. Short, imperative.
severity: bug-prevention             # Required. One of: bug-prevention, maintenance, style
enforcement: both                    # Required. One of: skill-only, lint-enforceable, both
confidence: strong                   # Required. One of: strong, moderate
tags: [type-safety, any]             # Required. Freeform array, at least one tag
related: [prefer-unknown]            # Optional. Array of other opinion IDs
lint:                                # Required if enforcement != skill-only
  type: existing                     # One of: existing, custom
  rule: "@typescript-eslint/no-explicit-any"  # Required if type: existing
---
```

Body sections (in order): `## Stance`, `## Why`, `## Do` (one or more), `## Don't` (one or more), `## Exceptions` (optional).

Code examples: 3-8 line TypeScript snippets. Multiple Do/Don't blocks allowed when pattern has distinct variations.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author Null Handling & Narrowing opinions (6) and Async & Promises opinions (5)</name>
  <files>
    docs/opinions/exhaustive-switch.md
    docs/opinions/prefer-optional-chaining.md
    docs/opinions/no-unnecessary-condition.md
    docs/opinions/prefer-nullish-coalescing.md
    docs/opinions/use-type-narrowing.md
    docs/opinions/strict-null-checks.md
    docs/opinions/no-floating-promises.md
    docs/opinions/no-misused-promises.md
    docs/opinions/require-await.md
    docs/opinions/return-await.md
    docs/opinions/prefer-async-await.md
  </files>
  <read_first>
    .planning/phases/01-opinion-foundation/01-RESEARCH.md
    .planning/phases/01-opinion-foundation/01-CONTEXT.md
    docs/opinions/README.md
    docs/opinions/INDEX.md
    docs/opinions/no-explicit-any.md
  </read_first>
  <action>
Create 11 opinion files. Read no-explicit-any.md first to match the established format exactly. Use the research enumeration for metadata.

**Null Handling & Narrowing opinions:**

1. `exhaustive-switch.md` -- id: exhaustive-switch, title: "Handle all cases in discriminated union switches", severity: bug-prevention, enforcement: both, confidence: strong, tags: [null-handling, narrowing, discriminated-unions], related: [exhaustive-discrimination, single-discriminant], lint: {type: existing, rule: "@typescript-eslint/switch-exhaustiveness-check"}
   - Stance: Every switch on a discriminated union must handle all variants. Use a `default: never` check to catch unhandled cases at compile time.
   - Why: Adding a new variant to a union without updating all switch statements causes silent bugs -- the new case falls through or hits a wrong default.
   - Do: switch with all cases handled plus `default: { const _exhaustive: never = value; }`
   - Don't: switch with a permissive default that swallows new variants

2. `prefer-optional-chaining.md` -- id: prefer-optional-chaining, title: "Use optional chaining over manual null checks", severity: style, enforcement: both, confidence: strong, tags: [null-handling, syntax], related: [prefer-nullish-coalescing, no-non-null-assertion], lint: {type: existing, rule: "@typescript-eslint/prefer-optional-chain"}
   - Stance: Use `?.` optional chaining instead of manual `&&` null checks.
   - Why: Optional chaining is more readable, less error-prone, and avoids the truthy/falsy pitfalls of `&&` chains.
   - Do: `const name = user?.profile?.name;`
   - Don't: `const name = user && user.profile && user.profile.name;`

3. `no-unnecessary-condition.md` -- id: no-unnecessary-condition, title: "Do not write conditions that are always true or false", severity: bug-prevention, enforcement: both, confidence: strong, tags: [null-handling, type-safety, dead-code], related: [strict-boolean-expressions], lint: {type: existing, rule: "@typescript-eslint/no-unnecessary-condition"}
   - Stance: Do not write conditions the type system can prove are always true or always false.
   - Why: An always-true condition is dead code that misleads readers. An always-false condition hides unreachable code. Both indicate a type mismatch between what the developer believes and what the compiler knows.
   - Do: only check for null when the type includes null (`string | null`)
   - Don't: `if (value !== null)` when value is typed as `string` (never null)

4. `prefer-nullish-coalescing.md` -- id: prefer-nullish-coalescing, title: "Use ?? instead of || for nullish fallbacks", severity: bug-prevention, enforcement: both, confidence: strong, tags: [null-handling, syntax], related: [prefer-optional-chaining, strict-boolean-expressions], lint: {type: existing, rule: "@typescript-eslint/prefer-nullish-coalescing"}
   - Stance: Use `??` (nullish coalescing) instead of `||` (logical OR) for default values.
   - Why: `||` falls through on `0`, `""`, and `false`, which are often valid values. `??` only falls through on `null` and `undefined`.
   - Do: `const port = config.port ?? 3000;` (preserves `0` if intentional)
   - Don't: `const port = config.port || 3000;` (treats `0` as missing)

5. `use-type-narrowing.md` -- id: use-type-narrowing, title: "Narrow types with guards instead of assertions", severity: bug-prevention, enforcement: skill-only, confidence: strong, tags: [null-handling, narrowing, type-safety], related: [no-type-assertions, exhaustive-switch]
   (No lint field -- skill-only)
   - Stance: Use type guard functions, `typeof`, `instanceof`, and `in` checks to narrow types. Do not use `as` assertions to "narrow."
   - Why: Type guards are checked at runtime -- if the check fails, you handle it. Assertions are unchecked -- if wrong, you crash.
   - Do: custom type guard function `function isUser(value: unknown): value is User`
   - Don't: `const user = value as User;` without any runtime check

6. `strict-null-checks.md` -- id: strict-null-checks, title: "Enable strictNullChecks (never disable)", severity: bug-prevention, enforcement: skill-only, confidence: strong, tags: [null-handling, tsconfig, strictness], related: [strict-tsconfig, prefer-nullish-coalescing]
   (No lint field -- skill-only, tsconfig setting)
   - Stance: Never set `strictNullChecks: false`. This flag is included in `strict: true` and must remain enabled.
   - Why: Without strictNullChecks, every type implicitly includes `null` and `undefined`. The compiler cannot catch null dereference bugs, which are the #1 source of runtime crashes.
   - Do: `"strict": true` in tsconfig (which enables strictNullChecks)
   - Don't: `"strictNullChecks": false` to silence null errors

**Async & Promises opinions:**

7. `no-floating-promises.md` -- id: no-floating-promises, title: "Always handle Promise rejections", severity: bug-prevention, enforcement: both, confidence: strong, tags: [async, promises], related: [no-misused-promises, return-await], lint: {type: existing, rule: "@typescript-eslint/no-floating-promises"}
   - Stance: Every Promise must be awaited, returned, or explicitly voided. Never leave a Promise floating.
   - Why: A floating Promise silently swallows rejections. The error vanishes, leaving the application in a corrupted state with no indication of failure.
   - Do: `await sendEmail(user);` or `void sendEmail(user);` (if intentionally fire-and-forget)
   - Don't: `sendEmail(user);` (floating -- rejection is lost)

8. `no-misused-promises.md` -- id: no-misused-promises, title: "Do not pass Promises where void is expected", severity: bug-prevention, enforcement: both, confidence: strong, tags: [async, promises], related: [no-floating-promises], lint: {type: existing, rule: "@typescript-eslint/no-misused-promises"}
   - Stance: Do not pass async functions to callbacks that expect synchronous void returns (event handlers, array methods, etc.).
   - Why: When an async function is passed where void is expected, the returned Promise is silently discarded. Errors in the async function vanish.
   - Do: wrap async logic in a non-async handler that explicitly catches errors
   - Don't: `button.addEventListener("click", async () => { await save(); });` (Promise returned to addEventListener which ignores it)

9. `require-await.md` -- id: require-await, title: "Do not mark functions async unless they use await", severity: maintenance, enforcement: both, confidence: moderate, tags: [async, promises], related: [no-floating-promises], lint: {type: existing, rule: "@typescript-eslint/require-await"}
   - Stance: Do not mark a function `async` unless it contains `await`. If it just returns a Promise, return it directly.
   - Why: `async` without `await` wraps the return value in an extra Promise layer unnecessarily. It also misleads readers into thinking the function has asynchronous operations.
   - Do: `function fetchData(): Promise<Data> { return api.get("/data"); }`
   - Don't: `async function fetchData(): Promise<Data> { return api.get("/data"); }`
   - Exceptions: Implementing an interface that requires an async signature.

10. `return-await.md` -- id: return-await, title: "Always return await in try/catch blocks", severity: bug-prevention, enforcement: both, confidence: strong, tags: [async, promises, error-handling], related: [no-floating-promises, typed-errors], lint: {type: existing, rule: "@typescript-eslint/return-await"}
    - Stance: Always use `return await` inside try/catch blocks. Outside try/catch, return the Promise directly.
    - Why: `return promise` inside try/catch exits the try block before the Promise settles -- the catch never fires on rejection. `return await promise` settles the Promise inside the try block.
    - Do: `try { return await fetchData(); } catch (e) { handleError(e); }`
    - Don't: `try { return fetchData(); } catch (e) { handleError(e); }` (catch never runs on rejection)

11. `prefer-async-await.md` -- id: prefer-async-await, title: "Use async/await over .then() chains", severity: maintenance, enforcement: skill-only, confidence: moderate, tags: [async, promises, readability], related: [no-floating-promises, return-await]
    (No lint field -- skill-only)
    - Stance: Use async/await for asynchronous control flow. Reserve `.then()` for simple one-step transformations.
    - Why: Async/await reads top-to-bottom like synchronous code. `.then()` chains nest, making error handling and debugging harder. Stack traces are clearer with await.
    - Do: `const data = await fetchData(); const result = process(data);`
    - Don't: `fetchData().then(data => process(data)).then(result => ...)`
    - Exceptions: `.then()` is fine for simple transforms like `promise.then(r => r.json())`.
  </action>
  <verify>
    <automated>ls docs/opinions/{exhaustive-switch,prefer-optional-chaining,no-unnecessary-condition,prefer-nullish-coalescing,use-type-narrowing,strict-null-checks,no-floating-promises,no-misused-promises,require-await,return-await,prefer-async-await}.md 2>/dev/null | wc -l | xargs test 11 -eq && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - 11 opinion files exist in docs/opinions/
    - exhaustive-switch.md contains `severity: bug-prevention` and `enforcement: both`
    - prefer-optional-chaining.md contains `severity: style`
    - use-type-narrowing.md contains `enforcement: skill-only` and does NOT contain `lint:`
    - strict-null-checks.md contains `enforcement: skill-only` and does NOT contain `lint:`
    - no-floating-promises.md contains `rule: "@typescript-eslint/no-floating-promises"`
    - prefer-async-await.md contains `enforcement: skill-only` and `confidence: moderate`
    - return-await.md contains `rule: "@typescript-eslint/return-await"`
    - Each file contains `## Stance`, `## Why`, `## Do`, `## Don't` headings
    - Each Do/Don't section contains a TypeScript code block
  </acceptance_criteria>
  <done>11 opinion files created covering null handling (6) and async (5) topics with valid frontmatter and body sections</done>
</task>

<task type="auto">
  <name>Task 2: Author Error Handling opinions (4) and Conditional Types opinion (1)</name>
  <files>
    docs/opinions/typed-errors.md
    docs/opinions/result-over-throw.md
    docs/opinions/error-discrimination.md
    docs/opinions/no-empty-catch.md
    docs/opinions/conditional-type-safety.md
  </files>
  <read_first>
    .planning/phases/01-opinion-foundation/01-RESEARCH.md
    docs/opinions/README.md
    docs/opinions/no-explicit-any.md
  </read_first>
  <action>
Create 5 opinion files. Read no-explicit-any.md to match the established format.

**Error Handling opinions:**

12. `typed-errors.md` -- id: typed-errors, title: "Throw only Error subclasses, never primitives", severity: bug-prevention, enforcement: both, confidence: strong, tags: [error-handling, type-safety], related: [use-unknown-in-catch, result-over-throw], lint: {type: existing, rule: "@typescript-eslint/only-throw-error"}
    - Stance: Only throw instances of `Error` or its subclasses. Never throw strings, numbers, or plain objects.
    - Why: Non-Error throwables lack stack traces, making debugging impossible. `catch` handlers that expect `error.message` or `error.stack` crash on primitives.
    - Do: `throw new Error("User not found");` or `throw new NotFoundError("User", id);`
    - Don't: `throw "User not found";` or `throw 404;`

13. `result-over-throw.md` -- id: result-over-throw, title: "Prefer Result types for expected failures, throw for bugs", severity: maintenance, enforcement: skill-only, confidence: moderate, tags: [error-handling, patterns, discriminated-unions], related: [typed-errors, error-discrimination]
    (No lint field -- skill-only)
    - Stance: Use a `Result<T, E>` discriminated union for expected, recoverable failures. Reserve `throw` for unexpected bugs and programmer errors.
    - Why: Thrown exceptions are invisible in function signatures -- callers do not know a function can fail. Result types make failure explicit in the type system, forcing callers to handle both cases.
    - Do: `type Result<T, E> = { ok: true; value: T } | { ok: false; error: E };` then `function parseConfig(raw: string): Result<Config, ParseError>`
    - Don't: `function parseConfig(raw: string): Config` that throws on invalid input (caller has no idea this can fail)
    - Exceptions: Functions where failure is truly exceptional (out of memory, network down) can throw.

14. `error-discrimination.md` -- id: error-discrimination, title: "Use discriminated union errors, not string messages", severity: maintenance, enforcement: skill-only, confidence: moderate, tags: [error-handling, discriminated-unions], related: [result-over-throw, typed-errors, single-discriminant]
    (No lint field -- skill-only)
    - Stance: Define error types as discriminated unions with a `type` field, not as strings or generic Error subclasses distinguished only by message.
    - Why: String messages are fragile to match against and break with wording changes. Discriminated unions let callers switch on `error.type` with full type narrowing and exhaustive checking.
    - Do: `type AppError = { type: "not-found"; id: string } | { type: "unauthorized"; reason: string };`
    - Don't: `throw new Error("NOT_FOUND: user 123");` then `if (error.message.startsWith("NOT_FOUND"))`

15. `no-empty-catch.md` -- id: no-empty-catch, title: "Do not silently swallow errors with empty catch blocks", severity: bug-prevention, enforcement: both, confidence: strong, tags: [error-handling], related: [typed-errors, use-unknown-in-catch], lint: {type: existing, rule: "no-empty"}
    NOTE: The ESLint core `no-empty` rule catches empty catch blocks. The lint field uses the core rule name without the `@typescript-eslint/` prefix.
    - Stance: Never leave a catch block empty. At minimum, log the error. Prefer explicit handling.
    - Why: An empty catch block silently swallows errors, making failures invisible. The application continues in a corrupted state with no indication of what went wrong.
    - Do: `catch (error) { logger.error("Failed to save:", error); }` or `catch (error) { /* Intentionally ignored: retry handles this */ }`
    - Don't: `catch (error) { }` or `catch { }`
    - Exceptions: If intentionally ignoring, add a comment explaining why.

**Conditional Types & Advanced Pitfalls (1 opinion):**

16. `conditional-type-safety.md` -- id: conditional-type-safety, title: "Wrap conditional types to prevent unintended distribution", severity: bug-prevention, enforcement: skill-only, confidence: moderate, tags: [advanced-types, conditional-types, generics], related: [constrain-generics, variance-annotations]
    (No lint field -- skill-only)
    - Stance: Wrap both sides of a conditional type in `[T]` (tuple) to prevent distributive behavior when it is not intended.
    - Why: Conditional types distribute over unions by default: `IsString<"a" | 1>` becomes `IsString<"a"> | IsString<1>` instead of evaluating the union as a whole. This causes unexpected results when the intent is to check the union itself.
    - Do: `type IsString<T> = [T] extends [string] ? true : false;` (evaluates the full union)
    - Don't: `type IsString<T> = T extends string ? true : false;` (distributes over each union member)
  </action>
  <verify>
    <automated>ls docs/opinions/{typed-errors,result-over-throw,error-discrimination,no-empty-catch,conditional-type-safety}.md 2>/dev/null | wc -l | xargs test 5 -eq && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - 5 opinion files exist in docs/opinions/
    - typed-errors.md contains `severity: bug-prevention` and `rule: "@typescript-eslint/only-throw-error"`
    - result-over-throw.md contains `enforcement: skill-only` and `confidence: moderate`
    - error-discrimination.md contains `enforcement: skill-only` and `tags:` includes `discriminated-unions`
    - no-empty-catch.md contains `rule: "no-empty"` (ESLint core rule, no @typescript-eslint prefix)
    - conditional-type-safety.md contains `enforcement: skill-only` and `tags:` includes `conditional-types`
    - result-over-throw.md body contains `type Result` or `Result<T, E>` in a code block
    - Each file contains `## Stance`, `## Why`, `## Do`, `## Don't` headings
    - Each Do/Don't section contains a code block
  </acceptance_criteria>
  <done>5 opinion files created covering error handling (4) and conditional types (1) with valid frontmatter and body sections</done>
</task>

</tasks>

<verification>
- All 16 files exist in docs/opinions/
- Every file has valid YAML frontmatter with required fields
- Every file has Stance, Why, Do, Don't sections
- Severity tiers match research: 11 bug-prevention, 4 maintenance, 1 style in this batch
- Enforcement tags match: 11 both, 5 skill-only
- Skill-only opinions have no lint field
- `related` fields reference valid opinion IDs from INDEX.md
</verification>

<success_criteria>
- 16 opinion files exist with correct frontmatter and body structure
- Code examples are TypeScript, 3-8 lines, and demonstrate the opinion clearly
- Cross-references in `related` fields are accurate
</success_criteria>

<output>
After completion, create `.planning/phases/01-opinion-foundation/01-03-SUMMARY.md`
</output>

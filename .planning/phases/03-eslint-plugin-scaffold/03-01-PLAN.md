---
phase: 03-eslint-plugin-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - eslint-plugin/package.json
  - eslint-plugin/tsconfig.json
  - eslint-plugin/tsup.config.ts
  - eslint-plugin/vitest.config.ts
  - eslint-plugin/tests/setup.ts
  - eslint-plugin/.gitignore
autonomous: true
requirements: [LINT-01, LINT-05]

must_haves:
  truths:
    - "Running npm install in eslint-plugin/ succeeds and installs all dependencies"
    - "Running npm run build produces CJS, ESM, and .d.ts outputs in dist/"
    - "Running npm test executes Vitest (even if no tests pass yet)"
  artifacts:
    - path: "eslint-plugin/package.json"
      provides: "Package manifest with correct name, exports, peer deps, scripts"
      contains: "eslint-plugin-typescript-narrows"
    - path: "eslint-plugin/tsconfig.json"
      provides: "TypeScript configuration for the plugin"
      contains: "compilerOptions"
    - path: "eslint-plugin/tsup.config.ts"
      provides: "Dual CJS/ESM build configuration"
      contains: "defineConfig"
    - path: "eslint-plugin/vitest.config.ts"
      provides: "Vitest test runner configuration"
      contains: "setupFiles"
    - path: "eslint-plugin/tests/setup.ts"
      provides: "RuleTester-to-Vitest wiring"
      contains: "RuleTester"
  key_links:
    - from: "eslint-plugin/package.json"
      to: "eslint-plugin/tsup.config.ts"
      via: "build script"
      pattern: '"build":\\s*"tsup"'
    - from: "eslint-plugin/vitest.config.ts"
      to: "eslint-plugin/tests/setup.ts"
      via: "setupFiles reference"
      pattern: "setupFiles.*setup"
---

<objective>
Scaffold the eslint-plugin/ package directory with all build tooling, test infrastructure, and package configuration so that subsequent plans can add source code into a working build/test pipeline.

Purpose: Establish the foundation that all plugin source code builds on -- package.json with correct exports/peers, tsup for dual output, Vitest wired to rule-tester.
Output: A buildable, testable (empty) package at eslint-plugin/.
</objective>

<execution_context>
@/Users/sethlivingston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sethlivingston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-eslint-plugin-scaffold/03-CONTEXT.md
@.planning/phases/03-eslint-plugin-scaffold/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package scaffold with build tooling</name>
  <files>eslint-plugin/package.json, eslint-plugin/tsconfig.json, eslint-plugin/tsup.config.ts, eslint-plugin/.gitignore</files>
  <read_first>
    .planning/phases/03-eslint-plugin-scaffold/03-RESEARCH.md
  </read_first>
  <action>
Create the `eslint-plugin/` directory at the repo root.

**eslint-plugin/package.json** -- Create with these exact values:
```json
{
  "name": "eslint-plugin-typescript-narrows",
  "version": "0.1.0",
  "description": "Opinionated ESLint plugin for TypeScript — one opinion per decision point",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.cjs"
    }
  },
  "files": ["dist"],
  "engines": {
    "node": ">=18.18.0"
  },
  "peerDependencies": {
    "eslint": "^9.0.0 || ^10.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint-plugin-import-x": "^4.0.0"
  },
  "dependencies": {
    "@typescript-eslint/utils": "^8.0.0",
    "typescript-eslint": "^8.0.0"
  },
  "devDependencies": {
    "@typescript-eslint/rule-tester": "^8.0.0",
    "eslint": "^10.0.0",
    "tsup": "^8.5.0",
    "typescript": "^5.5.0",
    "vitest": "^4.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint-plugin-import-x": "^4.0.0"
  },
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  }
}
```

**eslint-plugin/tsconfig.json** -- Create with:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "Node16",
    "moduleResolution": "Node16",
    "lib": ["ES2022"],
    "outDir": "dist",
    "rootDir": "src",
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "isolatedModules": true
  },
  "include": ["src"],
  "exclude": ["dist", "node_modules", "tests"]
}
```

**eslint-plugin/tsup.config.ts** -- Create with:
```typescript
import { defineConfig } from 'tsup';

export default defineConfig({
  entry: ['src/index.ts'],
  format: ['cjs', 'esm'],
  dts: true,
  clean: true,
  outDir: 'dist',
  target: 'node18',
  platform: 'node',
  splitting: false,
  sourcemap: false,
  external: [
    'eslint',
    /^@typescript-eslint/,
    /^typescript-eslint/,
    /^eslint-plugin-import/,
    'typescript',
  ],
  outExtension({ format }) {
    return {
      js: format === 'cjs' ? '.cjs' : '.mjs',
    };
  },
});
```

**eslint-plugin/.gitignore** -- Create with:
```
node_modules/
dist/
```

Then create a minimal `eslint-plugin/src/index.ts` stub so the build can succeed:
```typescript
export default {};
```

Run `cd eslint-plugin && npm install` to install all dependencies.
Run `cd eslint-plugin && npm run build` to verify the build pipeline works.
  </action>
  <verify>
    <automated>cd /Users/sethlivingston/dev/sethlivingston/the-typescript-narrows/eslint-plugin && npm run build && ls dist/index.cjs dist/index.mjs dist/index.d.ts</automated>
  </verify>
  <acceptance_criteria>
    - eslint-plugin/package.json contains `"name": "eslint-plugin-typescript-narrows"`
    - eslint-plugin/package.json contains `"type": "module"`
    - eslint-plugin/package.json contains `"eslint": "^9.0.0 || ^10.0.0"` in peerDependencies
    - eslint-plugin/package.json contains `"@typescript-eslint/parser": "^8.0.0"` in peerDependencies
    - eslint-plugin/package.json contains `"eslint-plugin-import-x": "^4.0.0"` in peerDependencies
    - eslint-plugin/tsup.config.ts contains `format: ['cjs', 'esm']`
    - eslint-plugin/tsup.config.ts contains `dts: true`
    - eslint-plugin/tsup.config.ts contains `external:` with eslint patterns
    - `npm run build` exits 0 and produces dist/index.cjs, dist/index.mjs, dist/index.d.ts
    - eslint-plugin/node_modules/ exists (npm install succeeded)
  </acceptance_criteria>
  <done>Package scaffold exists with working build producing CJS + ESM + .d.ts outputs</done>
</task>

<task type="auto">
  <name>Task 2: Create Vitest configuration and RuleTester setup</name>
  <files>eslint-plugin/vitest.config.ts, eslint-plugin/tests/setup.ts</files>
  <read_first>
    eslint-plugin/package.json
    .planning/phases/03-eslint-plugin-scaffold/03-RESEARCH.md
  </read_first>
  <action>
**eslint-plugin/vitest.config.ts** -- Create with:
```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    setupFiles: ['./tests/setup.ts'],
    globals: false,
  },
});
```

**eslint-plugin/tests/setup.ts** -- Create with:
```typescript
import * as vitest from 'vitest';
import { RuleTester } from '@typescript-eslint/rule-tester';

RuleTester.afterAll = vitest.afterAll;
RuleTester.it = vitest.it;
RuleTester.itOnly = vitest.it.only;
RuleTester.describe = vitest.describe;
```

Verify `npm test` runs without crashing (it should report 0 tests found, which is fine -- no test files exist yet).
  </action>
  <verify>
    <automated>cd /Users/sethlivingston/dev/sethlivingston/the-typescript-narrows/eslint-plugin && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - eslint-plugin/vitest.config.ts contains `setupFiles: ['./tests/setup.ts']`
    - eslint-plugin/tests/setup.ts contains `import { RuleTester } from '@typescript-eslint/rule-tester'`
    - eslint-plugin/tests/setup.ts contains `RuleTester.afterAll = vitest.afterAll`
    - eslint-plugin/tests/setup.ts contains `RuleTester.it = vitest.it`
    - eslint-plugin/tests/setup.ts contains `RuleTester.describe = vitest.describe`
    - `npx vitest run` exits without error (0 tests or no test files is acceptable)
  </acceptance_criteria>
  <done>Vitest configured with RuleTester wiring; test runner executes without errors</done>
</task>

</tasks>

<verification>
- `cd eslint-plugin && npm run build` exits 0
- `cd eslint-plugin && ls dist/index.cjs dist/index.mjs dist/index.d.ts` shows all three outputs
- `cd eslint-plugin && npx vitest run` exits without errors
- package.json has correct name, exports, peer deps
</verification>

<success_criteria>
The eslint-plugin/ directory exists as a fully scaffolded npm package with working build (CJS+ESM+dts) and test infrastructure (Vitest+RuleTester). No source code beyond a stub -- ready for Plan 02 to add plugin implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-eslint-plugin-scaffold/03-01-SUMMARY.md`
</output>

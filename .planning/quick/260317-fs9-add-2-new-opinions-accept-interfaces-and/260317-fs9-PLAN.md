---
phase: quick/260317-fs9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/opinions/accept-interfaces.md
  - docs/opinions/wrap-error-cause.md
  - docs/opinions/INDEX.md
  - skill/the-typescript-narrows/references/functions.md
  - skill/the-typescript-narrows/references/error-handling.md
  - skill/the-typescript-narrows/SKILL.md
autonomous: true
requirements: [add-accept-interfaces, add-wrap-error-cause]
must_haves:
  truths:
    - "accept-interfaces corpus file exists with frontmatter + Stance/Why/Do/Don't"
    - "wrap-error-cause corpus file exists with frontmatter + Stance/Why/Do/Don't"
    - "Both opinions appear in INDEX.md with correct severity/enforcement tags"
    - "Both opinions appear in their respective SKILL.md sections"
    - "Both reference files contain the new opinion entries"
    - "INDEX.md total updated to 58"
  artifacts:
    - path: "docs/opinions/accept-interfaces.md"
      provides: "Corpus file for accept-interfaces opinion"
      contains: "severity: maintenance"
    - path: "docs/opinions/wrap-error-cause.md"
      provides: "Corpus file for wrap-error-cause opinion"
      contains: "severity: maintenance"
    - path: "docs/opinions/INDEX.md"
      provides: "Master index with 58 opinions"
      contains: "58 opinions"
    - path: "skill/the-typescript-narrows/references/functions.md"
      provides: "Functions reference with accept-interfaces"
      contains: "Accept interfaces"
    - path: "skill/the-typescript-narrows/references/error-handling.md"
      provides: "Error handling reference with wrap-error-cause"
      contains: "Wrap errors with cause"
    - path: "skill/the-typescript-narrows/SKILL.md"
      provides: "Skill index with both new opinions"
      contains: "accept-interfaces"
  key_links:
    - from: "docs/opinions/INDEX.md"
      to: "docs/opinions/accept-interfaces.md"
      via: "markdown link"
      pattern: "\\[accept-interfaces\\]"
    - from: "docs/opinions/INDEX.md"
      to: "docs/opinions/wrap-error-cause.md"
      via: "markdown link"
      pattern: "\\[wrap-error-cause\\]"
---

<objective>
Add 2 new opinions to the corpus and skill: accept-interfaces and wrap-error-cause.

Purpose: Expand the opinion set with two Go-inspired patterns -- accepting interfaces in function params and wrapping errors with ES2022 cause.
Output: 2 corpus files, updated INDEX.md (56->58), updated reference files, updated SKILL.md index.
</objective>

<execution_context>
@/Users/sethlivingston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sethlivingston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/opinions/prefer-early-return.md (corpus pattern for skill-only opinion)
@docs/opinions/INDEX.md (master index -- update count and add entries)
@skill/the-typescript-narrows/SKILL.md (skill index -- add bullets)
@skill/the-typescript-narrows/references/functions.md (append accept-interfaces)
@skill/the-typescript-narrows/references/error-handling.md (append wrap-error-cause)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create corpus files for both opinions</name>
  <files>docs/opinions/accept-interfaces.md, docs/opinions/wrap-error-cause.md</files>
  <action>
Create docs/opinions/accept-interfaces.md following the exact pattern from prefer-early-return.md:

Frontmatter:
- id: accept-interfaces
- title: "Accept interfaces, return concrete types"
- severity: maintenance
- enforcement: skill-only
- confidence: strong
- tags: [functions, parameters, return-types, dependency-inversion]
- related: [prefer-interface, explicit-return-types]
- No lint field (skill-only opinion omits it per project convention)

Content sections:
- Stance: Accept interfaces in function parameters; return concrete types. Keep inputs narrow and outputs concrete.
- Why: Go-inspired "accept interfaces, return structs." Narrow input interfaces make functions testable (pass a mock that satisfies only the interface) and reusable (any conforming type works). Concrete return types give callers full type information without casting. This is dependency inversion at the type level.
- Do: Show a function that accepts a narrow interface param (e.g., `Readable` with just the methods needed) and returns a concrete type. Scope to functions only (not constructors or React props per user decision). 3-8 lines.
- Don't: Show a function that accepts and returns the full concrete class. 3-8 lines.

Create docs/opinions/wrap-error-cause.md following the same pattern:

Frontmatter:
- id: wrap-error-cause
- title: "Wrap errors with cause when rethrowing"
- severity: maintenance
- enforcement: skill-only
- confidence: strong
- tags: [error-handling, debugging, error-cause]
- related: [typed-errors, no-empty-catch]
- No lint field (skill-only)

Content sections:
- Stance: When catching and rethrowing errors, wrap with `new Error('context', { cause: err })`. Never rethrow bare or discard the original.
- Why: Go-inspired error wrapping (fmt.Errorf with %w). The cause chain preserves the full error trail for debugging while each layer adds context about what operation failed. Without cause, you lose the original stack trace and error details. ES2022 Error cause is the standard mechanism -- no custom classes needed.
- Do: Show try/catch that wraps with `{ cause: err }`. ES2022 Error cause only (per user decision). 3-8 lines.
- Don't: Show bare rethrow (`throw err`) or discarding original (`throw new Error('failed')`). 3-8 lines.
  </action>
  <verify>
    <automated>test -f docs/opinions/accept-interfaces.md && test -f docs/opinions/wrap-error-cause.md && grep -q "severity: maintenance" docs/opinions/accept-interfaces.md && grep -q "cause" docs/opinions/wrap-error-cause.md && echo "PASS"</automated>
  </verify>
  <done>Both corpus files exist with correct frontmatter (severity: maintenance, enforcement: skill-only, no lint field) and all four sections (Stance/Why/Do/Don't) with 3-8 line code examples</done>
</task>

<task type="auto">
  <name>Task 2: Update skill reference files, SKILL.md index, and INDEX.md</name>
  <files>skill/the-typescript-narrows/references/functions.md, skill/the-typescript-narrows/references/error-handling.md, skill/the-typescript-narrows/SKILL.md, docs/opinions/INDEX.md</files>
  <action>
1. Append to skill/the-typescript-narrows/references/functions.md:
   - Add `---` separator after existing content
   - Add new section following the exact pattern of existing entries (see error-handling.md for multi-opinion reference file pattern):
     - `## Accept interfaces, return concrete types`
     - **Stance:** / **Why:** / **Do:** (code block) / **Don't:** (code block)
   - Content derived from the corpus file created in Task 1, condensed to reference format

2. Append to skill/the-typescript-narrows/references/error-handling.md:
   - Add `---` separator after the last opinion (Never swallow errors)
   - Add new section:
     - `## Wrap errors with cause when rethrowing`
     - **Stance:** / **Why:** / **Do:** (code block) / **Don't:** (code block)
   - Content derived from the corpus file created in Task 1

3. Update skill/the-typescript-narrows/SKILL.md:
   - In the **Functions** section, add a new bullet after the arrow functions bullet:
     `- Accept interfaces in function parameters; return concrete types. [M]`
   - In the **Error Handling** section, add a new bullet after the empty catch bullet:
     `- Wrap errors with `{ cause: err }` when rethrowing; never discard the original error. [M]`

4. Update docs/opinions/INDEX.md:
   - In the **Functions** section, add:
     `- [accept-interfaces](accept-interfaces.md) -- Accept interfaces, return concrete types [M] [skill-only]`
   - In the **Error Handling** section, add:
     `- [wrap-error-cause](wrap-error-cause.md) -- Wrap errors with cause when rethrowing [M] [skill-only]`
   - Update header line: "56 opinions" -> "58 opinions"
   - Update Summary section: Total opinions 56 -> 58, update Maintenance count 23 -> 25, Skill-only count 19 -> 21
  </action>
  <verify>
    <automated>grep -q "accept-interfaces" skill/the-typescript-narrows/SKILL.md && grep -q "wrap-error-cause" docs/opinions/INDEX.md && grep -q "58 opinions" docs/opinions/INDEX.md && grep -q "Accept interfaces" skill/the-typescript-narrows/references/functions.md && grep -q "Wrap errors with cause" skill/the-typescript-narrows/references/error-handling.md && echo "PASS"</automated>
  </verify>
  <done>All 4 files updated: both reference files have new opinion sections, SKILL.md has new bullets in Functions and Error Handling sections, INDEX.md has new entries and counts updated to 58 total / 25 maintenance / 21 skill-only</done>
</task>

</tasks>

<verification>
- All 6 files exist and contain expected content
- INDEX.md counts are consistent (58 total, 25 maintenance, 21 skill-only)
- No lint field appears in either corpus file frontmatter
- Both corpus files use skill-only enforcement
- Code examples are 3-8 lines each
- accept-interfaces scoped to functions only (no constructor/React props mentions)
- wrap-error-cause uses ES2022 Error cause only (no custom error classes)
</verification>

<success_criteria>
- 2 new corpus files in docs/opinions/ with correct frontmatter and content
- INDEX.md updated with 2 new entries, total 58
- SKILL.md updated with 2 new opinion bullets in correct sections
- Both reference files updated with new opinion sections
- All patterns match existing conventions exactly
</success_criteria>

<output>
After completion, create `.planning/quick/260317-fs9-add-2-new-opinions-accept-interfaces-and/260317-fs9-SUMMARY.md`
</output>

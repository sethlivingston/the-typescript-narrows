---
phase: quick/260317-r4a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - eslint-plugin/package.json
  - eslint-plugin/package-lock.json
  - eslint-plugin/src/index.ts
  - eslint-plugin/README.md
  - README.md
autonomous: true
requirements: [scope-eslint-plugin-name]
must_haves:
  truths:
    - "npm package name is @sethlivingston/eslint-plugin-typescript-narrows"
    - "ESLint plugin meta.name matches the scoped package name"
    - "README install commands and import statements use the scoped name"
    - "ESLint rule prefix typescript-narrows/ is unchanged"
  artifacts:
    - path: "eslint-plugin/package.json"
      provides: "Scoped package name"
      contains: "@sethlivingston/eslint-plugin-typescript-narrows"
    - path: "eslint-plugin/src/index.ts"
      provides: "Plugin meta with scoped name"
      contains: "@sethlivingston/eslint-plugin-typescript-narrows"
    - path: "eslint-plugin/README.md"
      provides: "Updated install and import instructions"
      contains: "@sethlivingston/eslint-plugin-typescript-narrows"
  key_links:
    - from: "eslint-plugin/package.json"
      to: "eslint-plugin/src/index.ts"
      via: "meta.name matches package name"
      pattern: "@sethlivingston/eslint-plugin-typescript-narrows"
---

<objective>
Scope the ESLint plugin npm package name to @sethlivingston.

Purpose: Establishes ownership under the @sethlivingston npm scope, preventing name squatting and following npm conventions for personal packages.
Output: All references to the package name updated from `eslint-plugin-typescript-narrows` to `@sethlivingston/eslint-plugin-typescript-narrows`.
</objective>

<execution_context>
@/Users/sethlivingston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sethlivingston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Key note: The ESLint rule prefix `typescript-narrows/` used in config files (strict.ts) and tests
is the plugin namespace within ESLint, NOT the npm package name. This prefix MUST NOT change.
Only the npm package name and its references in documentation/meta change.

Files that reference the package name (exhaustive list):
- eslint-plugin/package.json — "name" field
- eslint-plugin/src/index.ts — meta.name
- eslint-plugin/README.md — title, install command, import statement
- README.md — root repo reference

Files that use rule prefix "typescript-narrows/" (DO NOT CHANGE):
- eslint-plugin/src/configs/strict.ts
- eslint-plugin/tests/configs/strict.test.ts
- eslint-plugin/tests/integration/smoke.test.ts
- eslint-plugin/src/utils/create-rule.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update package name and plugin meta to scoped name</name>
  <files>eslint-plugin/package.json, eslint-plugin/package-lock.json, eslint-plugin/src/index.ts</files>
  <action>
1. In eslint-plugin/package.json: Change "name" from "eslint-plugin-typescript-narrows" to "@sethlivingston/eslint-plugin-typescript-narrows". No other fields change.

2. In eslint-plugin/src/index.ts: Change meta.name from 'eslint-plugin-typescript-narrows' to '@sethlivingston/eslint-plugin-typescript-narrows'.

3. In eslint-plugin/: Run `npm install --package-lock-only` to regenerate package-lock.json with the new name. Do NOT run full install if node_modules already exists and dependencies haven't changed.

IMPORTANT: Do NOT change the rule prefix "typescript-narrows/" anywhere. The ESLint plugin namespace is independent of the npm scope.
  </action>
  <verify>
    <automated>cd /Users/sethlivingston/dev/sethlivingston/the-typescript-narrows/eslint-plugin && node -e "const p = require('./package.json'); if (p.name !== '@sethlivingston/eslint-plugin-typescript-narrows') { process.exit(1); }" && grep -q "@sethlivingston/eslint-plugin-typescript-narrows" src/index.ts && cd /Users/sethlivingston/dev/sethlivingston/the-typescript-narrows/eslint-plugin && npm run build && npm run test</automated>
  </verify>
  <done>package.json name is "@sethlivingston/eslint-plugin-typescript-narrows", meta.name matches, build and tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Update README references to use scoped package name</name>
  <files>eslint-plugin/README.md, README.md</files>
  <action>
1. In eslint-plugin/README.md:
   - Title: Change "# eslint-plugin-typescript-narrows" to "# @sethlivingston/eslint-plugin-typescript-narrows"
   - Install command: Change to `npm install --save-dev @sethlivingston/eslint-plugin-typescript-narrows`
   - Import statement: Change to `import tsnarrows from '@sethlivingston/eslint-plugin-typescript-narrows';`

2. In README.md (root):
   - Update the ESLint Plugin bullet: Change "`eslint-plugin-typescript-narrows`" to "`@sethlivingston/eslint-plugin-typescript-narrows`"

Do NOT change any other text, links, or descriptions.
  </action>
  <verify>
    <automated>cd /Users/sethlivingston/dev/sethlivingston/the-typescript-narrows && grep -c "@sethlivingston/eslint-plugin-typescript-narrows" eslint-plugin/README.md | grep -q "3" && grep -c "@sethlivingston/eslint-plugin-typescript-narrows" README.md | grep -q "1"</automated>
  </verify>
  <done>eslint-plugin/README.md has 3 occurrences of the scoped name (title, install, import), root README.md has 1 occurrence</done>
</task>

</tasks>

<verification>
- `cd eslint-plugin && npm run build && npm run test` passes (rule prefix unchanged)
- `grep -r "eslint-plugin-typescript-narrows" eslint-plugin/package.json eslint-plugin/src/index.ts eslint-plugin/README.md README.md` shows ONLY scoped references (all prefixed with @sethlivingston/)
- No unscoped "eslint-plugin-typescript-narrows" remains in the modified files
</verification>

<success_criteria>
- npm package name is @sethlivingston/eslint-plugin-typescript-narrows in package.json
- Plugin meta.name matches the scoped package name
- All README install/import instructions use the scoped name
- Build and all tests pass without modification (rule prefix unchanged)
</success_criteria>

<output>
After completion, create `.planning/quick/260317-r4a-scope-the-eslint-plugin-to-sethlivingsto/260317-r4a-SUMMARY.md`
</output>

---
phase: quick/260317-f68
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/opinions/prefer-for-of.md
  - docs/opinions/prefer-early-return.md
  - docs/opinions/prefer-arrow-functions.md
  - docs/opinions/explicit-return-types.md
  - docs/opinions/prefer-undefined.md
  - docs/opinions/no-reduce.md
  - docs/opinions/INDEX.md
  - skill/the-typescript-narrows/references/null-handling.md
  - skill/the-typescript-narrows/references/type-safety.md
  - skill/the-typescript-narrows/references/iteration.md
  - skill/the-typescript-narrows/references/control-flow.md
  - skill/the-typescript-narrows/references/functions.md
  - skill/the-typescript-narrows/SKILL.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "6 new opinion files exist in docs/opinions/ with correct frontmatter and all required sections"
    - "INDEX.md lists 56 opinions across updated topic groups with correct summary stats"
    - "SKILL.md lists 54 opinions (48 + 6 new, accounting for prior merges) across updated topic groups"
    - "Each new opinion has a corresponding entry in the appropriate skill reference file"
  artifacts:
    - path: "docs/opinions/prefer-for-of.md"
      provides: "prefer-for-of opinion"
      contains: "id: prefer-for-of"
    - path: "docs/opinions/no-reduce.md"
      provides: "no-reduce opinion"
      contains: "id: no-reduce"
    - path: "docs/opinions/prefer-early-return.md"
      provides: "prefer-early-return opinion"
      contains: "id: prefer-early-return"
    - path: "docs/opinions/prefer-arrow-functions.md"
      provides: "prefer-arrow-functions opinion"
      contains: "id: prefer-arrow-functions"
    - path: "docs/opinions/explicit-return-types.md"
      provides: "explicit-return-types opinion"
      contains: "id: explicit-return-types"
    - path: "docs/opinions/prefer-undefined.md"
      provides: "prefer-undefined opinion"
      contains: "id: prefer-undefined"
    - path: "skill/the-typescript-narrows/references/iteration.md"
      provides: "Iteration and Transforms reference"
    - path: "skill/the-typescript-narrows/references/control-flow.md"
      provides: "Control Flow reference"
    - path: "skill/the-typescript-narrows/references/functions.md"
      provides: "Functions reference"
  key_links:
    - from: "docs/opinions/INDEX.md"
      to: "docs/opinions/*.md"
      via: "markdown links"
      pattern: "\\[.*\\]\\(.*\\.md\\)"
    - from: "skill/the-typescript-narrows/SKILL.md"
      to: "skill/the-typescript-narrows/references/*.md"
      via: "reference links"
      pattern: "\\[references/.*\\.md\\]"
---

<objective>
Add 6 new opinions (prefer-for-of, prefer-early-return, prefer-arrow-functions, explicit-return-types, prefer-undefined, no-reduce) to the corpus, carry each forward to skill reference files, and update both INDEX.md and SKILL.md indexes.

Purpose: Expand the opinion corpus with commonly-needed TypeScript guidance that was identified as missing.
Output: 6 corpus files, 3 new skill reference files, 2 updated reference files, updated INDEX.md and SKILL.md.
</objective>

<execution_context>
@/Users/sethlivingston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sethlivingston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/quick/260317-f68-add-new-opinions-foreach-vs-for-of-async/260317-f68-CONTEXT.md
@docs/opinions/README.md
@docs/opinions/INDEX.md
@docs/opinions/ban-enums.md (example corpus file format)
@docs/opinions/prefer-async-await.md (example skill-only corpus file)
@skill/the-typescript-narrows/SKILL.md
@skill/the-typescript-narrows/references/type-safety.md (example reference file format)
@skill/the-typescript-narrows/references/null-handling.md (will be updated with prefer-undefined)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 6 opinion corpus files and update INDEX.md</name>
  <files>
    docs/opinions/prefer-for-of.md,
    docs/opinions/no-reduce.md,
    docs/opinions/prefer-early-return.md,
    docs/opinions/prefer-arrow-functions.md,
    docs/opinions/explicit-return-types.md,
    docs/opinions/prefer-undefined.md,
    docs/opinions/INDEX.md
  </files>
  <action>
Create 6 opinion files following the exact frontmatter and body format from README.md and existing examples (ban-enums.md, prefer-async-await.md). Each file has YAML frontmatter (id, title, severity, enforcement, confidence, tags, related, lint if applicable) and body sections (Stance, Why, Do, Don't, Exceptions if applicable). Code examples: 3-8 lines each.

**prefer-for-of.md:**
- id: prefer-for-of, title: "Use for...of over .forEach", severity: maintenance, enforcement: skill-only, confidence: strong
- tags: [iteration, loops, async], related: [no-reduce, prefer-async-await]
- Stance: Use `for...of` loops instead of `.forEach()`.
- Why: `.forEach` swallows `break`, `continue`, and `return` (only exits the callback, not the enclosing function). It cannot use `await` -- the callback runs synchronously even if marked async, producing fire-and-forget promises. `for...of` with `const`/`let` gives a fresh binding per iteration, so the closure safety myth about `.forEach` does not hold.
- Do: `for (const item of items) { await process(item); }` (show async case)
- Don't: `items.forEach(async (item) => { await process(item); });` (shows swallowed await)
- Exception: None (per user decision -- no exceptions for forEach)

**no-reduce.md:**
- id: no-reduce, title: "Ban .reduce entirely", severity: maintenance, enforcement: skill-only, confidence: strong
- tags: [iteration, functional, readability], related: [prefer-for-of]
- Stance: Do not use `.reduce()`. Use `for...of` or `.map`/`.filter` combinations.
- Why: `.reduce` forces the reader to mentally simulate an accumulator through every iteration. A `for...of` loop with a mutable accumulator or a `.map`/`.filter` chain expresses the same logic more directly. No "trivial case" exception -- zero ambiguity.
- Do: Show a before/after where for...of is clearly more readable (e.g., summing values or building an object from array)
- Don't: Show the .reduce version of the same operation

**prefer-early-return.md:**
- id: prefer-early-return, title: "Fail fast with early returns", severity: maintenance, enforcement: skill-only, confidence: strong
- tags: [control-flow, readability, guard-clauses], related: []
- Stance: Return early for preconditions and error cases. Keep the happy path left-aligned.
- Why: Deeply nested if/else blocks obscure the main logic path. Guard clauses at the top of a function handle edge cases first, letting the reader focus on the happy path without tracking nesting levels. This is the Go-style guard clause pattern -- handle errors at the gate, not in the basement.
- Do: Show function with guard clauses returning early, happy path at bottom
- Don't: Show same function with deeply nested if/else

**prefer-arrow-functions.md:**
- id: prefer-arrow-functions, title: "Use arrow functions by default", severity: style, enforcement: skill-only, confidence: moderate
- tags: [functions, style, declarations], related: []
- Stance: Use arrow functions by default. Use function declarations only when hoisting is needed.
- Why: Arrow functions do not rebind `this`, eliminating an entire class of bugs. They are more concise for callbacks and inline usage. Function declarations are justified for mutual recursion or top-level organization where a function must be callable before its definition.
- Do: `const processItem = (item: Item): Result => { ... }`
- Don't: `function processItem(item: Item): Result { ... }` (when hoisting is not needed)
- Exceptions: Function declarations are appropriate when hoisting is required (mutual recursion, top-level organization before definition).

**explicit-return-types.md:**
- id: explicit-return-types, title: "Require explicit return types on exported functions", severity: maintenance, enforcement: both, confidence: strong
- tags: [type-safety, api-stability, declarations], related: []
- lint: { type: existing, rule: "@typescript-eslint/explicit-function-return-type" }
- Stance: Annotate explicit return types on all exported functions. Internal functions can rely on inference.
- Why: Inferred return types change silently when implementation changes, breaking consumers. Explicit return types on exports lock the API contract -- the compiler will catch unintended changes before they ship.
- Do: `export const fetchUser = async (id: string): Promise<User> => { ... }`
- Don't: `export const fetchUser = async (id: string) => { ... }` (inferred return type)
- Exceptions: Internal/private functions where type inference is sufficient and the function is not part of a public API.

**prefer-undefined.md:**
- id: prefer-undefined, title: "Prefer undefined over null", severity: style, enforcement: skill-only, confidence: moderate
- tags: [null-handling, style, consistency], related: [prefer-nullish-coalescing, strict-null-checks]
- Stance: Use `undefined` over `null`. Match JavaScript's natural defaults.
- Why: JavaScript uses `undefined` for missing parameters, void returns, optional properties, and uninitialized variables. Using `null` introduces a second "nothing" value that must be checked separately. Picking one reduces the number of states to handle.
- Do: `function find(id: string): User | undefined { ... }`
- Don't: `function find(id: string): User | null { ... }`
- Exceptions: When interfacing with APIs that use `null` explicitly (JSON parse results, DOM APIs like `document.getElementById`).

**Update INDEX.md:**
Add two new topic groups and one addition to existing group:

1. Add new "Iteration and Transforms" group (after "Async & Promises" or before "Error Handling") with prefer-for-of and no-reduce.
2. Add new "Control Flow" group with prefer-early-return.
3. Add new "Functions" group with prefer-arrow-functions.
4. Add explicit-return-types to "Type Declarations" group.
5. Add prefer-undefined to "Null Handling & Narrowing" group.

Update Summary section:
- Total opinions: 56 (was 50)
- Recalculate severity counts: Bug prevention stays 24, Maintenance adds 4 (prefer-for-of, no-reduce, prefer-early-return, explicit-return-types) = 23, Style adds 2 (prefer-arrow-functions, prefer-undefined) = 9. Wait -- current is B:24, M:19, S:7 = 50. Adding: M:+4 (prefer-for-of, no-reduce, prefer-early-return, explicit-return-types), S:+2 (prefer-arrow-functions, prefer-undefined) = B:24, M:23, S:9 = 56.
- Both (skill + lint): 37 (was 36, +1 for explicit-return-types), Skill-only: 19 (was 14, +5 for the other five).
  </action>
  <verify>
    <automated>test $(ls docs/opinions/prefer-for-of.md docs/opinions/no-reduce.md docs/opinions/prefer-early-return.md docs/opinions/prefer-arrow-functions.md docs/opinions/explicit-return-types.md docs/opinions/prefer-undefined.md 2>/dev/null | wc -l) -eq 6 && grep -c "Total opinions.*56" docs/opinions/INDEX.md</automated>
  </verify>
  <done>6 opinion files exist with correct frontmatter (id, title, severity, enforcement, confidence, tags) and all required body sections (Stance, Why, Do, Don't). INDEX.md lists all 56 opinions with correct summary stats.</done>
</task>

<task type="auto">
  <name>Task 2: Create/update skill reference files and update SKILL.md index</name>
  <files>
    skill/the-typescript-narrows/references/iteration.md,
    skill/the-typescript-narrows/references/control-flow.md,
    skill/the-typescript-narrows/references/functions.md,
    skill/the-typescript-narrows/references/null-handling.md,
    skill/the-typescript-narrows/references/type-declarations.md,
    skill/the-typescript-narrows/references/type-safety.md,
    skill/the-typescript-narrows/SKILL.md
  </files>
  <action>
Follow the exact reference file format from type-safety.md and null-handling.md: each opinion gets an h2 heading, then Stance/Why/Do/Don't sections (bold labels, not h3). Code examples 3-8 lines. Opinions separated by `---` horizontal rules.

**Create skill/the-typescript-narrows/references/iteration.md** (new file):
Title: "# Iteration and Transforms"
Two opinions: prefer-for-of, no-reduce. Follow the Stance/Why/Do/Don't format from type-safety.md.

**Create skill/the-typescript-narrows/references/control-flow.md** (new file):
Title: "# Control Flow"
One opinion: prefer-early-return.

**Create skill/the-typescript-narrows/references/functions.md** (new file):
Title: "# Functions"
One opinion: prefer-arrow-functions. Include the Exceptions section.

**Update skill/the-typescript-narrows/references/type-declarations.md:**
Read the existing file first. Append explicit-return-types opinion at the end (after last `---` separator). Follow identical format to existing entries.

**Update skill/the-typescript-narrows/references/null-handling.md:**
Read the existing file first. Append prefer-undefined opinion at the end (after last `---` separator). Follow identical format to existing entries.

Note: type-safety.md does NOT need updating. explicit-return-types goes in type-declarations.md since it's about declaration patterns, not runtime type safety.

**Update SKILL.md:**
Read the existing file first. Add new sections and entries:

1. Add "## Iteration and Transforms" section (after "Async and Promises") with:
   - Use `for...of` over `.forEach()`; forEach swallows break/continue/return/await. [M]
   - Do not use `.reduce()`; use `for...of` or `.map`/`.filter` combinations. [M]
   - Link: `For rationale and examples: [references/iteration.md](references/iteration.md)`

2. Add "## Control Flow" section (after "Error Handling") with:
   - Return early for preconditions; keep the happy path left-aligned. [M]
   - Link: `For rationale and examples: [references/control-flow.md](references/control-flow.md)`

3. Add "## Functions" section (after "Control Flow") with:
   - Use arrow functions by default; use function declarations only when hoisting is needed. [S]
   - Link: `For rationale and examples: [references/functions.md](references/functions.md)`

4. Add to "## Type Declarations" section:
   - Require explicit return types on exported functions; internal functions can rely on inference. [M]

5. Add to "## Null Handling and Narrowing" section:
   - Prefer `undefined` over `null`; match JavaScript's natural defaults. [S]

IMPORTANT: Each opinion bullet in SKILL.md is a plain bullet (no bold), with severity tag [B]/[M]/[S] at the end. No other formatting. Match existing bullet style exactly.

The final SKILL.md opinion count should be 54 (the skill merged ban-enums/no-const-enum and exhaustive-switch/exhaustive-discrimination, so 50 corpus -> 48 skill, plus 6 new = 54 skill opinions from 56 corpus opinions).
  </action>
  <verify>
    <automated>test -f skill/the-typescript-narrows/references/iteration.md && test -f skill/the-typescript-narrows/references/control-flow.md && test -f skill/the-typescript-narrows/references/functions.md && grep -c "Iteration and Transforms" skill/the-typescript-narrows/SKILL.md && grep -c "Control Flow" skill/the-typescript-narrows/SKILL.md && grep -c "Functions" skill/the-typescript-narrows/SKILL.md</automated>
  </verify>
  <done>3 new reference files exist (iteration.md, control-flow.md, functions.md). 2 existing reference files updated (null-handling.md, type-declarations.md). SKILL.md has 54 opinions across all topic groups including 3 new sections (Iteration and Transforms, Control Flow, Functions) and 2 additions to existing sections (Type Declarations, Null Handling and Narrowing). All bullets are plain text with [B]/[M]/[S] severity tags.</done>
</task>

</tasks>

<verification>
- All 6 opinion files have valid YAML frontmatter with required fields (id, title, severity, enforcement, confidence, tags)
- All 6 opinion files have required body sections (Stance, Why, Do, Don't)
- INDEX.md total is 56, with correct severity and enforcement breakdowns
- SKILL.md has 54 opinions (count bullets with severity tags)
- Every new opinion in INDEX.md has a corresponding entry in SKILL.md and a skill reference file
- No existing opinions were modified or removed
- Reference file format matches existing files (bold labels, code blocks, --- separators)
</verification>

<success_criteria>
- 6 new opinion files in docs/opinions/ with correct format
- INDEX.md shows 56 total opinions with accurate stats
- SKILL.md shows 54 opinions with 3 new topic groups
- 3 new reference files + 2 updated reference files in skill/the-typescript-narrows/references/
- All cross-references (INDEX links, SKILL reference links) are valid
</success_criteria>

<output>
After completion, create `.planning/quick/260317-f68-add-new-opinions-foreach-vs-for-of-async/260317-f68-SUMMARY.md`
</output>

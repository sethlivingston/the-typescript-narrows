---
phase: quick/260317-j2m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/add-opinion.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "When a new opinion has enforcement: both and lint.type: existing, the command adds the rule to eslint-plugin/src/configs/strict.ts"
    - "When a new opinion has enforcement: both and lint.type: custom, the command creates a rule file, test file, wires into rules/index.ts, and adds to strict.ts"
    - "The command does NOT attempt ESLint changes for skill-only opinions"
  artifacts:
    - path: ".claude/commands/add-opinion.md"
      provides: "Complete add-opinion command with ESLint integration steps"
  key_links:
    - from: ".claude/commands/add-opinion.md"
      to: "eslint-plugin/src/"
      via: "Steps 5-6 in command"
      pattern: "eslint-plugin/src"
---

<objective>
Update the /add-opinion local command to ensure that when a new opinion has ESLint enforcement (enforcement: both), the command carries the opinion all the way through to the ESLint plugin -- adding existing rules to the strict config or scaffolding custom rules with tests.

Purpose: Currently the command stops at the skill layer (Steps 1-4). Opinions with lint enforcement are not wired into the ESLint plugin, meaning a manual follow-up is needed. This makes the command incomplete for the most common use case.

Output: Updated .claude/commands/add-opinion.md with Steps 5-6 for ESLint integration.
</objective>

<execution_context>
@/Users/sethlivingston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sethlivingston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/commands/add-opinion.md
@eslint-plugin/src/index.ts
@eslint-plugin/src/rules/index.ts
@eslint-plugin/src/rules/ban-enums.ts
@eslint-plugin/src/configs/strict.ts
@eslint-plugin/tests/rules/ban-enums.test.ts
@eslint-plugin/src/utils/create-rule.ts

<interfaces>
<!-- Existing ESLint plugin structure the command must target -->

eslint-plugin/src/rules/index.ts exports:
  export const rules = { 'ban-enums': banEnums, 'ban-barrel-files': banBarrelFiles };

eslint-plugin/src/configs/strict.ts exports:
  export function createStrictConfig(plugin: ESLint.Plugin): Linter.Config[]
  Rules are organized in sections: ESLint core, @typescript-eslint (in strict-type-checked), @typescript-eslint (explicitly added), typescript-narrows custom

eslint-plugin/src/rules/ban-enums.ts pattern:
  import { createRule } from '../utils/create-rule.js';
  export const banEnums = createRule({ name, meta: { type, docs: { description, opinionId, recommended, requiresTypeChecking }, messages, schema }, defaultOptions: [], create(context) { ... } });

eslint-plugin/tests/rules/ban-enums.test.ts pattern:
  Uses vitest + @typescript-eslint/rule-tester
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ESLint integration steps to add-opinion command</name>
  <files>.claude/commands/add-opinion.md</files>
  <action>
Read the current add-opinion.md command. After the existing Step 4 (Update SKILL.md) and before Step 5 (Commit), insert two new conditional steps. Renumber the old Step 5 (Commit) to Step 7.

**New Step 5: Add rule to strict config (when enforcement is "both" and lint.type is "existing")**

Condition: Only run when the opinion has `enforcement: both` AND `lint.type: existing`.

Instructions:
1. Read `eslint-plugin/src/configs/strict.ts`
2. Add the ESLint rule (from `lint.rule` field) to the appropriate section in the rules object:
   - If rule starts with `@typescript-eslint/` and is already in tseslint.configs.strictTypeChecked, add to the "@typescript-eslint rules already in strict-type-checked" section with explicit `'error'` override
   - If rule starts with `@typescript-eslint/` and is NOT in strictTypeChecked, add to the "@typescript-eslint rules NOT in strict-type-checked" section
   - If rule starts with `import/`, add to the import plugin rules section
   - If rule has no prefix, add to the "ESLint core rules" section
3. If the rule requires configuration options (from the opinion's lint field or known rule documentation), include the options array. Otherwise use just `'error'`.
4. Update the comment counts in the section headers (e.g., "ESLint core rules (3)" becomes "(4)")

**New Step 6: Create custom ESLint rule (when enforcement is "both" and lint.type is "custom")**

Condition: Only run when the opinion has `enforcement: both` AND `lint.type: custom`.

Instructions:
1. **Create rule file** at `eslint-plugin/src/rules/{id}.ts`:
   - Follow the exact pattern from `ban-enums.ts`: import createRule, export a named constant (camelCase version of the id), define meta with type/docs/messages/schema, implement create() visitor
   - Set `docs.opinionId` to the opinion id
   - Set `docs.requiresTypeChecking` based on whether the rule needs type information
   - Write the AST visitor logic based on the opinion's stance and examples

2. **Create test file** at `eslint-plugin/tests/rules/{id}.test.ts`:
   - Follow the exact pattern from `ban-enums.test.ts`
   - Include at least 2x as many valid cases as invalid cases
   - Valid cases should cover normal usage that the rule should NOT flag
   - Invalid cases should cover the patterns the opinion bans/requires, with messageId assertions

3. **Wire into rules index** at `eslint-plugin/src/rules/index.ts`:
   - Add import statement following existing pattern (import from `./{id}.js`)
   - Add entry to the rules object: `'{id}': {camelCaseName}`

4. **Add to strict config** at `eslint-plugin/src/configs/strict.ts`:
   - Add `'typescript-narrows/{id}': 'error'` to the typescript-narrows custom rules section
   - Update the comment count

5. **Run tests** to verify: `cd eslint-plugin && npx vitest run tests/rules/{id}.test.ts`

Also update Step 0 (Infer opinion fields) to also infer these additional fields when lint.type is "custom":
- **lint.requiresTypeChecking**: boolean (does the rule need type info?)
- **lint.messageId**: short camelCase identifier for the error message (e.g., "banned", "preferUnknown")
- **lint.message**: the error message string

Include these in the Step 0c review preview when lint.type is "custom".

Finally, update the Notes section to add:
- When creating custom rules, reference existing rules in `eslint-plugin/src/rules/` for patterns
- Custom rule tests must have at least 2x valid cases compared to invalid cases
- Run `cd eslint-plugin && npx vitest run` after all changes to verify nothing is broken
  </action>
  <verify>
    <automated>grep -c "Step 5\|Step 6\|Step 7" .claude/commands/add-opinion.md</automated>
  </verify>
  <done>The add-opinion.md command contains Steps 5 (existing rule config), 6 (custom rule creation), and 7 (commit, renumbered from old Step 5). The command handles all three enforcement paths: skill-only (skips Steps 5-6), both+existing (runs Step 5), both+custom (runs Step 6).</done>
</task>

</tasks>

<verification>
- Read the updated .claude/commands/add-opinion.md and confirm Steps 5, 6, 7 exist
- Confirm the command references the correct file paths (eslint-plugin/src/rules/, eslint-plugin/tests/rules/, eslint-plugin/src/configs/strict.ts)
- Confirm skill-only opinions are not affected (Steps 5-6 are conditional)
</verification>

<success_criteria>
- The /add-opinion command includes conditional ESLint integration steps
- Step 5 handles lint.type: existing (config-only change)
- Step 6 handles lint.type: custom (rule file + test file + wiring + config)
- Step 7 is the renumbered commit step that also stages ESLint files when applicable
- The command flow is clear: infer fields -> review -> corpus -> INDEX -> skill ref -> SKILL.md -> (optional ESLint) -> commit
</success_criteria>

<output>
After completion, create `.planning/quick/260317-j2m-update-add-opinion-command-to-carry-opin/260317-j2m-01-SUMMARY.md`
</output>

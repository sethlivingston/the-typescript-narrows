---
phase: 06-fix-skill-rename-cascade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate-traceability.mjs
  - README.md
  - CLAUDE.md
  - .claude/commands/add-opinion.md
  - eslint-plugin/README.md
  - docs/TRACEABILITY.md
autonomous: true
requirements: [COVR-01, COVR-02, COVR-03, SKIL-18, SKIL-21]

must_haves:
  truths:
    - "generate-traceability.mjs runs without errors on the new directory structure"
    - "All README links resolve to correct paths after rename"
    - "plugin.json name field is consistent with SKILL.md name field"
    - "Developer workflows (add-opinion command) reference correct paths"
    - "Traceability matrix covers all 59 opinions"
    - "All path fixes and directory rename are committed atomically"
  artifacts:
    - path: "scripts/generate-traceability.mjs"
      provides: "Traceability script with correct skill reference path"
      contains: "skills/typescript-narrows/references"
    - path: "README.md"
      provides: "Project landing page with correct skill link"
      contains: "skills/typescript-narrows/SKILL.md"
    - path: "eslint-plugin/README.md"
      provides: "ESLint plugin README with correct companion plugin link"
      contains: "the-typescript-narrows/skills/typescript-narrows/SKILL.md"
    - path: ".claude/commands/add-opinion.md"
      provides: "Developer workflow with correct skill paths"
      contains: "skills/typescript-narrows/"
    - path: "docs/TRACEABILITY.md"
      provides: "Regenerated traceability matrix"
  key_links:
    - from: "scripts/generate-traceability.mjs"
      to: "plugin/the-typescript-narrows/skills/typescript-narrows/references/"
      via: "path join in loadSkillReferences()"
      pattern: "skills.*typescript-narrows.*references"
    - from: "plugin/the-typescript-narrows/.claude-plugin/plugin.json"
      to: "plugin/the-typescript-narrows/skills/typescript-narrows/SKILL.md"
      via: "name field matching"
      pattern: "typescript-narrows"
---

<objective>
Fix all broken paths from the skill directory rename (the-typescript-narrows -> typescript-narrows), regenerate traceability, and commit atomically.

Purpose: The skill directory was renamed but paths in scripts, READMEs, and developer commands still reference the old location. This phase makes the rename official and ensures nothing is broken.
Output: All paths updated, traceability regenerated, single atomic commit.
</objective>

<execution_context>
@/Users/sethlivingston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sethlivingston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-fix-skill-rename-cascade/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all broken path references from skill rename</name>
  <files>scripts/generate-traceability.mjs, README.md, eslint-plugin/README.md, .claude/commands/add-opinion.md</files>
  <read_first>
    - scripts/generate-traceability.mjs (line 113 has the broken path)
    - README.md (line 27 has the broken link)
    - eslint-plugin/README.md (line 40 has broken companion plugin link)
    - .claude/commands/add-opinion.md (lines 155 and 185 have broken paths)
    - CLAUDE.md (check for any old paths in repo layout section)
    - plugin/the-typescript-narrows/.claude-plugin/plugin.json (verify name field is already "typescript-narrows")
    - plugin/the-typescript-narrows/skills/typescript-narrows/SKILL.md (verify name frontmatter is "typescript-narrows")
  </read_first>
  <action>
    Fix the following broken path references:

    1. **scripts/generate-traceability.mjs line 113**: Replace `skills/the-typescript-narrows` with `skills/typescript-narrows`. Change the path in loadSkillReferences() from:
       `join(ROOT, 'plugin', 'the-typescript-narrows', 'skills', 'the-typescript-narrows', 'references')`
       to:
       `join(ROOT, 'plugin', 'the-typescript-narrows', 'skills', 'typescript-narrows', 'references')`

    2. **README.md line 27**: Change the Claude Plugin link from:
       `plugin/the-typescript-narrows/skills/the-typescript-narrows/SKILL.md`
       to:
       `plugin/the-typescript-narrows/skills/typescript-narrows/SKILL.md`
       (both the display path in backticks AND the markdown link target)

    3. **eslint-plugin/README.md line 40**: Fix the companion plugin link. The current path is `../plugin/typescript-narrows/skills/typescript-narrows/SKILL.md` which is wrong — the parent directory is `the-typescript-narrows` not `typescript-narrows`. Change to:
       `../plugin/the-typescript-narrows/skills/typescript-narrows/SKILL.md`
       Note: This is a different bug — the parent directory name was incorrectly shortened, not the skill subdirectory.

    4. **.claude/commands/add-opinion.md line 155**: Change the reference file path from:
       `plugin/the-typescript-narrows/skills/the-typescript-narrows/references/{new-topic}.md`
       to:
       `plugin/the-typescript-narrows/skills/typescript-narrows/references/{new-topic}.md`

    5. **.claude/commands/add-opinion.md line 185**: Change the SKILL.md path from:
       `plugin/the-typescript-narrows/skills/the-typescript-narrows/SKILL.md`
       to:
       `plugin/the-typescript-narrows/skills/typescript-narrows/SKILL.md`

    6. **CLAUDE.md**: Read the file and check for any references to the old path. The current repo layout section says `plugin/` generically, which is fine. If any old `skills/the-typescript-narrows` paths exist, update them. If none exist, no changes needed.

    7. **Verify consistency**: Confirm plugin.json name field ("typescript-narrows") matches SKILL.md frontmatter name ("typescript-narrows"). Both should already be correct — just verify, do not change.

    After all edits, run TWO grep checks to confirm no broken references remain in non-planning files:
    ```
    # Check 1: No old skill subdirectory references
    grep -r "skills/the-typescript-narrows" --include="*.md" --include="*.mjs" --include="*.ts" --include="*.json" . | grep -v ".planning/"

    # Check 2: No broken parent directory references (plugin/typescript-narrows/ without the- prefix)
    grep -r "plugin/typescript-narrows" --include="*.md" --include="*.mjs" --include="*.ts" --include="*.json" . | grep -v ".planning/" | grep -v "plugin/the-typescript-narrows"
    ```
    Both commands should return no results.
  </action>
  <verify>
    <automated>cd /Users/sethlivingston/dev/sethlivingston/the-typescript-narrows && grep -r "skills/the-typescript-narrows" --include="*.md" --include="*.mjs" --include="*.ts" --include="*.json" . | grep -v ".planning/" | wc -l | xargs test 0 -eq && grep -r "plugin/typescript-narrows" --include="*.md" --include="*.mjs" --include="*.ts" --include="*.json" . | grep -v ".planning/" | grep -v "plugin/the-typescript-narrows" | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>All path references updated from old skill directory name to new, including eslint-plugin/README.md companion link. No stale references remain outside of .planning/ historical records.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate traceability matrix and validate coverage</name>
  <files>docs/TRACEABILITY.md</files>
  <read_first>
    - scripts/generate-traceability.mjs (full file — to understand what it generates)
    - docs/TRACEABILITY.md (current state before regeneration)
    - docs/opinions/INDEX.md (source of truth — 59 opinions)
  </read_first>
  <action>
    Run the traceability generation script to regenerate docs/TRACEABILITY.md with the corrected paths:

    ```
    cd /Users/sethlivingston/dev/sethlivingston/the-typescript-narrows && node scripts/generate-traceability.mjs
    ```

    The script must exit with code 0 (no errors). If it fails, diagnose and fix the issue in generate-traceability.mjs before retrying.

    After successful generation, read the output docs/TRACEABILITY.md and verify:
    - The matrix lists all 59 opinions from docs/opinions/INDEX.md
    - Each opinion has at least one artifact mapping (skill reference, lint rule, or both)
    - No opinion shows as "unmapped" or "missing"

    If coverage gaps are found (opinions not mapped to any artifact), investigate whether:
    - The opinion exists in a skill reference file but the script didn't find it (path issue)
    - The opinion genuinely has no skill or lint coverage (needs to be added — but flag this rather than silently ignoring)
  </action>
  <verify>
    <automated>cd /Users/sethlivingston/dev/sethlivingston/the-typescript-narrows && node scripts/generate-traceability.mjs && echo "EXIT_CODE: $?"</automated>
  </verify>
  <done>Traceability matrix regenerated successfully from corrected paths. Script runs cleanly. All 59 opinions have artifact mappings.</done>
</task>

<task type="auto">
  <name>Task 3: Stage and commit all changes atomically</name>
  <files></files>
  <action>
    Create a single atomic commit containing the skill directory rename and all cascading path fixes.

    IMPORTANT: `eslint-plugin/README.md` and `eslint-plugin/package.json` have pre-existing staged changes from a prior task. Before committing, unstage those two files so they are not accidentally bundled into this commit:

    ```
    git reset HEAD eslint-plugin/README.md eslint-plugin/package.json
    ```

    Then stage ONLY the rename and path-fix changes:

    1. Stage the directory rename (old deletions + new additions):
       ```
       git add plugin/the-typescript-narrows/skills/the-typescript-narrows/
       git add plugin/the-typescript-narrows/skills/typescript-narrows/
       ```

    2. Stage all modified path-fix files:
       ```
       git add scripts/generate-traceability.mjs README.md eslint-plugin/README.md .claude/commands/add-opinion.md docs/TRACEABILITY.md
       ```
       (Only add CLAUDE.md if it was actually modified in Task 1.)

    3. Stage plugin.json (already has staged change for name field):
       ```
       git add plugin/the-typescript-narrows/.claude-plugin/plugin.json
       ```

    4. Verify `eslint-plugin/package.json` is NOT staged (it should only show as unstaged/modified):
       ```
       git status
       ```

    5. Commit:
       ```
       git commit -m "refactor(plugin): rename skill directory and fix all cascading paths

    Rename skills/the-typescript-narrows -> skills/typescript-narrows.
    Update all references in scripts, READMEs, commands, and plugin metadata.
    Regenerate traceability matrix against new paths."
       ```

    6. Verify git detected the rename:
       ```
       git show --stat HEAD
       ```
       Output should show `rename` entries for the skill directory files.
  </action>
  <verify>
    <automated>cd /Users/sethlivingston/dev/sethlivingston/the-typescript-narrows && git log -1 --oneline | grep -q "rename skill directory" && git show --stat HEAD | grep -q "plugin/the-typescript-narrows/skills" && git diff --name-only --cached | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>All rename and path-fix changes committed atomically. eslint-plugin/package.json is NOT included in the commit. Git history shows a clean rename + path update commit.</done>
</task>

</tasks>

<verification>
1. `grep -r "skills/the-typescript-narrows" --include="*.md" --include="*.mjs" --include="*.ts" --include="*.json" . | grep -v ".planning/"` returns zero results
2. `grep -r "plugin/typescript-narrows" --include="*.md" --include="*.mjs" --include="*.ts" --include="*.json" . | grep -v ".planning/" | grep -v "plugin/the-typescript-narrows"` returns zero results
3. `node scripts/generate-traceability.mjs` exits with code 0
4. `plugin.json` name field matches `SKILL.md` name field (both "typescript-narrows")
5. All README links point to paths that exist on disk
6. `git log -1` shows the atomic commit with rename + all path fixes
7. `eslint-plugin/package.json` is not included in the commit
</verification>

<success_criteria>
- Zero stale path references to old skill directory name in runtime/workflow files
- Zero broken parent directory references (plugin/typescript-narrows without the- prefix)
- Traceability script runs without errors on new directory structure
- Traceability matrix regenerated and covers all 59 opinions
- plugin.json and SKILL.md name fields are consistent
- All changes committed atomically in a single commit
- Pre-existing eslint-plugin/package.json staged changes NOT bundled into this commit
</success_criteria>

<output>
After completion, create `.planning/phases/06-fix-skill-rename-cascade/06-01-SUMMARY.md`
</output>

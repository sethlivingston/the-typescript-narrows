---
phase: 02-claude-skill
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - skill/the-typescript-narrows/references/error-handling.md
  - skill/the-typescript-narrows/references/immutability.md
  - skill/the-typescript-narrows/references/modules.md
  - skill/the-typescript-narrows/references/naming.md
autonomous: true
requirements: [SKIL-07, SKIL-08, SKIL-11, SKIL-13, SKIL-14, SKIL-10]

must_haves:
  truths:
    - "An AI agent loading error-handling.md gets Result type and typed error guidance"
    - "An AI agent loading immutability.md gets readonly-by-default patterns"
    - "An AI agent loading modules.md gets named exports and barrel file ban guidance"
    - "An AI agent loading naming.md gets PascalCase/camelCase rules and boolean naming"
    - "Every opinion includes a Why rationale the agent can relay to developers"
    - "Every opinion includes Do and Don't code examples"
  artifacts:
    - path: "skill/the-typescript-narrows/references/error-handling.md"
      provides: "4 error handling opinions in skill-optimized format"
      contains: "## Throw only Error subclasses"
    - path: "skill/the-typescript-narrows/references/immutability.md"
      provides: "5 immutability opinions in skill-optimized format"
      contains: "## Use `const` by default"
    - path: "skill/the-typescript-narrows/references/modules.md"
      provides: "4 module organization opinions in skill-optimized format"
      contains: "## Use named exports"
    - path: "skill/the-typescript-narrows/references/naming.md"
      provides: "3 naming convention opinions in skill-optimized format"
      contains: "## PascalCase"
  key_links:
    - from: "skill/the-typescript-narrows/SKILL.md"
      to: "skill/the-typescript-narrows/references/error-handling.md"
      via: "relative markdown link in Error Handling section"
      pattern: "references/error-handling\\.md"
---

<objective>
Author reference files for Error Handling, Immutability, Modules, and Naming topics (16 opinions total).

Purpose: These reference files complete the middle tier of opinions -- error patterns, immutability defaults, module organization, and naming conventions. Each file is independently loadable for focused topic guidance.

Output: 4 reference files in `skill/the-typescript-narrows/references/`
</objective>

<execution_context>
@/Users/sethlivingston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sethlivingston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-claude-skill/02-CONTEXT.md
@.planning/phases/02-claude-skill/02-RESEARCH.md

<interfaces>
From skill/the-typescript-narrows/SKILL.md (created by plan 02-01):
- Links to references/error-handling.md, references/immutability.md, references/modules.md, references/naming.md
- Each topic section lists opinion stances that reference files must expand on
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author error-handling.md and immutability.md reference files</name>
  <files>skill/the-typescript-narrows/references/error-handling.md, skill/the-typescript-narrows/references/immutability.md</files>
  <read_first>
    - skill/the-typescript-narrows/SKILL.md (which opinions listed under Error Handling and Immutability)
    - skill/the-typescript-narrows/references/type-safety.md (established format reference from plan 02-02)
    - docs/opinions/typed-errors.md
    - docs/opinions/result-over-throw.md
    - docs/opinions/error-discrimination.md
    - docs/opinions/no-empty-catch.md
    - docs/opinions/prefer-const.md
    - docs/opinions/no-var.md
    - docs/opinions/prefer-readonly.md
    - docs/opinions/prefer-readonly-params.md
    - docs/opinions/no-mutable-exports.md
  </read_first>
  <action>
Create 2 reference files using the established format (H1 title, H2 per opinion with Stance/Why/Do/Don't/optional Exception, separated by `---`).

**error-handling.md** -- Title: `# Error Handling`. Contains 4 opinions:
1. typed-errors: "Throw only Error subclasses, never primitives"
2. result-over-throw: "Prefer Result types for expected failures, throw for bugs"
3. error-discrimination: "Use discriminated union errors, not string messages"
4. no-empty-catch: "Do not silently swallow errors with empty catch blocks"

**immutability.md** -- Title: `# Immutability and Const`. Contains 5 opinions:
1. prefer-const: "Use `const` by default; `let` only when reassigned"
2. no-var: "Never use `var`"
3. prefer-readonly: "Mark class properties `readonly` when not reassigned"
4. prefer-readonly-params: "Use `Readonly<T>` and `ReadonlyArray<T>` in function params"
5. no-mutable-exports: "Do not export mutable bindings"

Same content transformation rules: imperative voice, tight prose, 3-8 line code examples, no framework content. Read corpus opinion files for source material but rewrite for skill-optimized format.
  </action>
  <verify>
    <automated>grep -c '^\#\# ' skill/the-typescript-narrows/references/error-handling.md | xargs -I{} test {} -eq 4 && echo "error-handling PASS: 4 opinions" || echo "error-handling FAIL: expected 4 H2 headings"; grep -c '^\#\# ' skill/the-typescript-narrows/references/immutability.md | xargs -I{} test {} -eq 5 && echo "immutability PASS: 5 opinions" || echo "immutability FAIL: expected 5 H2 headings"</automated>
  </verify>
  <acceptance_criteria>
    - skill/the-typescript-narrows/references/error-handling.md exists and starts with `# Error Handling`
    - error-handling.md contains exactly 4 H2 headings
    - error-handling.md contains 4 occurrences of `**Stance:**`
    - error-handling.md contains 4 occurrences of `**Why:**`
    - error-handling.md contains `## Throw only Error subclasses` (typed-errors)
    - error-handling.md contains `## Prefer Result types` (result-over-throw)
    - skill/the-typescript-narrows/references/immutability.md exists and starts with `# Immutability`
    - immutability.md contains exactly 5 H2 headings
    - immutability.md contains 5 occurrences of `**Stance:**`
    - immutability.md contains `## Never use `var`` or similar (no-var)
    - No framework-specific content in either file
  </acceptance_criteria>
  <done>error-handling.md has 4 opinions and immutability.md has 5 opinions in skill-optimized format with Stance, Why, Do, Don't sections.</done>
</task>

<task type="auto">
  <name>Task 2: Author modules.md and naming.md reference files</name>
  <files>skill/the-typescript-narrows/references/modules.md, skill/the-typescript-narrows/references/naming.md</files>
  <read_first>
    - skill/the-typescript-narrows/SKILL.md (which opinions listed under Module Organization and Naming Conventions)
    - skill/the-typescript-narrows/references/type-safety.md (established format reference)
    - docs/opinions/named-exports-only.md
    - docs/opinions/ban-barrel-files.md
    - docs/opinions/explicit-imports.md
    - docs/opinions/no-circular-deps.md
    - docs/opinions/naming-convention.md
    - docs/opinions/no-hungarian-notation.md
    - docs/opinions/boolean-naming.md
  </read_first>
  <action>
Create 2 reference files using the established format.

**modules.md** -- Title: `# Module Organization`. Contains 4 opinions:
1. named-exports-only: "Use named exports; ban default exports"
2. ban-barrel-files: "Do not use barrel files (index.ts re-exports)"
3. explicit-imports: "Import from the source module, not through re-exports"
4. no-circular-deps: "Avoid circular dependencies"

**naming.md** -- Title: `# Naming Conventions`. Contains 3 opinions:
1. naming-convention: "PascalCase types, camelCase values, UPPER_CASE constants"
2. no-hungarian-notation: "Do not prefix type names (no `IUser`, `TConfig`, `EStatus`)"
3. boolean-naming: "Prefix booleans with `is`, `has`, `should`, `can`"

Same content transformation rules as previous tasks.
  </action>
  <verify>
    <automated>grep -c '^\#\# ' skill/the-typescript-narrows/references/modules.md | xargs -I{} test {} -eq 4 && echo "modules PASS: 4 opinions" || echo "modules FAIL: expected 4 H2 headings"; grep -c '^\#\# ' skill/the-typescript-narrows/references/naming.md | xargs -I{} test {} -eq 3 && echo "naming PASS: 3 opinions" || echo "naming FAIL: expected 3 H2 headings"</automated>
  </verify>
  <acceptance_criteria>
    - skill/the-typescript-narrows/references/modules.md exists and starts with `# Module Organization`
    - modules.md contains exactly 4 H2 headings
    - modules.md contains 4 occurrences of `**Stance:**`
    - modules.md contains `## Use named exports` (named-exports-only)
    - modules.md contains `## Do not use barrel files` (ban-barrel-files)
    - skill/the-typescript-narrows/references/naming.md exists and starts with `# Naming Conventions`
    - naming.md contains exactly 3 H2 headings
    - naming.md contains 3 occurrences of `**Stance:**`
    - naming.md contains `## PascalCase` or `PascalCase types` in an H2 heading
    - naming.md contains `## Prefix booleans` or `boolean` in an H2 heading
    - No framework-specific content in either file
  </acceptance_criteria>
  <done>modules.md has 4 opinions and naming.md has 3 opinions in skill-optimized format with Stance, Why, Do, Don't sections.</done>
</task>

</tasks>

<verification>
- Each reference file has the correct number of H2 opinion headings
- Each opinion has **Stance:**, **Why:**, **Do:**, **Don't:** sections
- `grep -ric 'react\|next\|express\|angular\|vue' skill/the-typescript-narrows/references/error-handling.md skill/the-typescript-narrows/references/immutability.md skill/the-typescript-narrows/references/modules.md skill/the-typescript-narrows/references/naming.md` returns 0
- Total opinions across 4 files: 4 + 5 + 4 + 3 = 16
</verification>

<success_criteria>
4 reference files exist with 16 total opinions in skill-optimized format. Every opinion has Stance, Why, Do, Don't sections. No framework-specific content. Format matches the pattern established by plan 02-02.
</success_criteria>

<output>
After completion, create `.planning/phases/02-claude-skill/02-03-SUMMARY.md`
</output>

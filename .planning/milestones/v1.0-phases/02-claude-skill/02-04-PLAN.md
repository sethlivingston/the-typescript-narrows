---
phase: 02-claude-skill
plan: 04
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - skill/the-typescript-narrows/references/discriminated-unions.md
  - skill/the-typescript-narrows/references/generics.md
  - skill/the-typescript-narrows/references/tsconfig-advanced.md
autonomous: true
requirements: [SKIL-12, SKIL-15, SKIL-16, SKIL-17, SKIL-09, SKIL-10]

must_haves:
  truths:
    - "An AI agent loading discriminated-unions.md gets single-discriminant and exhaustive handling guidance"
    - "An AI agent loading generics.md gets constraint patterns, overload guidance, variance annotations, and branded type guidance"
    - "An AI agent loading tsconfig-advanced.md gets strict tsconfig flags and conditional type pitfalls"
    - "Every opinion includes a Why rationale the agent can relay to developers"
    - "Every opinion includes Do and Don't code examples"
  artifacts:
    - path: "skill/the-typescript-narrows/references/discriminated-unions.md"
      provides: "3 discriminated union opinions in skill-optimized format"
      contains: "## Use a single"
    - path: "skill/the-typescript-narrows/references/generics.md"
      provides: "5 opinions (generics + branded types + variance) in skill-optimized format"
      contains: "## Always constrain generic"
    - path: "skill/the-typescript-narrows/references/tsconfig-advanced.md"
      provides: "3 opinions (tsconfig + conditional types) in skill-optimized format"
      contains: "## Enable `strict: true`"
  key_links:
    - from: "skill/the-typescript-narrows/SKILL.md"
      to: "skill/the-typescript-narrows/references/generics.md"
      via: "relative markdown link in Generics section"
      pattern: "references/generics\\.md"
---

<objective>
Author reference files for Discriminated Unions, Generics (including Branded Types and Variance), and tsconfig/Advanced Pitfalls topics (11 opinions total).

Purpose: These reference files cover the advanced/differentiator opinions -- discriminated unions, generics, branded types, variance annotations, tsconfig strictness, and conditional type pitfalls. The branded-types and variance-annotations opinions are merged into generics.md; tsconfig and conditional-type-safety are merged into tsconfig-advanced.md (per research recommendation to combine small topic groups).

Output: 3 reference files in `skill/the-typescript-narrows/references/`
</objective>

<execution_context>
@/Users/sethlivingston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sethlivingston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-claude-skill/02-CONTEXT.md
@.planning/phases/02-claude-skill/02-RESEARCH.md

<interfaces>
From skill/the-typescript-narrows/SKILL.md (created by plan 02-01):
- Links to references/discriminated-unions.md, references/generics.md, references/tsconfig-advanced.md
- Each topic section lists opinion stances that reference files must expand on
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author discriminated-unions.md reference file</name>
  <files>skill/the-typescript-narrows/references/discriminated-unions.md</files>
  <read_first>
    - skill/the-typescript-narrows/SKILL.md (which opinions listed under Discriminated Unions)
    - skill/the-typescript-narrows/references/type-safety.md (established format reference from plan 02-02)
    - docs/opinions/single-discriminant.md
    - docs/opinions/no-destructure-before-narrow.md
    - docs/opinions/exhaustive-discrimination.md
  </read_first>
  <action>
Create reference file using the established format (H1 title, H2 per opinion with Stance/Why/Do/Don't/optional Exception, separated by `---`).

**discriminated-unions.md** -- Title: `# Discriminated Unions`. Contains 3 opinions:
1. single-discriminant: "Use a single `type` or `kind` field as discriminant"
2. no-destructure-before-narrow: "Do not destructure discriminated unions before narrowing"
3. exhaustive-discrimination: "Always handle all variants of a discriminated union"

Same content transformation rules as previous plans: imperative voice, tight prose, 3-8 line code examples, no framework content.
  </action>
  <verify>
    <automated>grep -c '^\#\# ' skill/the-typescript-narrows/references/discriminated-unions.md | xargs -I{} test {} -eq 3 && echo "discriminated-unions PASS: 3 opinions" || echo "discriminated-unions FAIL: expected 3 H2 headings"</automated>
  </verify>
  <acceptance_criteria>
    - skill/the-typescript-narrows/references/discriminated-unions.md exists and starts with `# Discriminated Unions`
    - File contains exactly 3 H2 headings
    - File contains 3 occurrences of `**Stance:**`
    - File contains 3 occurrences of `**Why:**`
    - File contains `## Use a single` (single-discriminant)
    - File contains `## Do not destructure` (no-destructure-before-narrow)
    - File contains `## Always handle all variants` or similar (exhaustive-discrimination)
    - No framework-specific content
  </acceptance_criteria>
  <done>discriminated-unions.md has 3 opinions in skill-optimized format with Stance, Why, Do, Don't sections.</done>
</task>

<task type="auto">
  <name>Task 2: Author generics.md and tsconfig-advanced.md reference files</name>
  <files>skill/the-typescript-narrows/references/generics.md, skill/the-typescript-narrows/references/tsconfig-advanced.md</files>
  <read_first>
    - skill/the-typescript-narrows/SKILL.md (which opinions listed under Generics and tsconfig)
    - skill/the-typescript-narrows/references/type-safety.md (established format reference)
    - docs/opinions/constrain-generics.md
    - docs/opinions/no-unnecessary-generics.md
    - docs/opinions/prefer-generics-over-overloads.md
    - docs/opinions/variance-annotations.md
    - docs/opinions/branded-types.md
    - docs/opinions/strict-tsconfig.md
    - docs/opinions/no-unchecked-index.md
    - docs/opinions/conditional-type-safety.md
  </read_first>
  <action>
Create 2 reference files using the established format.

**generics.md** -- Title: `# Generics and Advanced Types`. Contains 5 opinions (merging "Generics & Advanced Types", "Branded & Nominal Types", and variance-annotations into one file):
1. constrain-generics: "Always constrain generic type parameters"
2. no-unnecessary-generics: "Do not add type parameters used only once"
3. prefer-generics-over-overloads: "Use generics instead of function overloads when possible"
4. variance-annotations: "Use `in`/`out` variance annotations on generic classes"
5. branded-types: "Use branded types for domain IDs and value objects"

**tsconfig-advanced.md** -- Title: `# tsconfig and Advanced Pitfalls`. Contains 3 opinions (merging "tsconfig Strictness" and "Conditional Types & Advanced Pitfalls"):
1. strict-tsconfig: "Enable `strict: true` and all strict family flags"
2. no-unchecked-index: "Enable `noUncheckedIndexedAccess`"
3. conditional-type-safety: "Wrap conditional types to prevent unintended distribution"

Same content transformation rules as previous plans.
  </action>
  <verify>
    <automated>grep -c '^\#\# ' skill/the-typescript-narrows/references/generics.md | xargs -I{} test {} -eq 5 && echo "generics PASS: 5 opinions" || echo "generics FAIL: expected 5 H2 headings"; grep -c '^\#\# ' skill/the-typescript-narrows/references/tsconfig-advanced.md | xargs -I{} test {} -eq 3 && echo "tsconfig-advanced PASS: 3 opinions" || echo "tsconfig-advanced FAIL: expected 3 H2 headings"</automated>
  </verify>
  <acceptance_criteria>
    - skill/the-typescript-narrows/references/generics.md exists and starts with `# Generics`
    - generics.md contains exactly 5 H2 headings
    - generics.md contains 5 occurrences of `**Stance:**`
    - generics.md contains 5 occurrences of `**Why:**`
    - generics.md contains `## Always constrain generic` (constrain-generics)
    - generics.md contains `branded` in an H2 heading (branded-types opinion)
    - generics.md contains `variance` in an H2 heading (variance-annotations opinion)
    - skill/the-typescript-narrows/references/tsconfig-advanced.md exists and starts with `# tsconfig`
    - tsconfig-advanced.md contains exactly 3 H2 headings
    - tsconfig-advanced.md contains 3 occurrences of `**Stance:**`
    - tsconfig-advanced.md contains `## Enable` (strict-tsconfig)
    - tsconfig-advanced.md contains `conditional` (case-insensitive) somewhere in the file
    - No framework-specific content in either file
  </acceptance_criteria>
  <done>generics.md has 5 opinions and tsconfig-advanced.md has 3 opinions in skill-optimized format with Stance, Why, Do, Don't sections.</done>
</task>

</tasks>

<verification>
- Each reference file has the correct number of H2 opinion headings
- Each opinion has **Stance:**, **Why:**, **Do:**, **Don't:** sections
- `grep -ric 'react\|next\|express\|angular\|vue' skill/the-typescript-narrows/references/discriminated-unions.md skill/the-typescript-narrows/references/generics.md skill/the-typescript-narrows/references/tsconfig-advanced.md` returns 0
- Total opinions across 3 files: 3 + 5 + 3 = 11
</verification>

<success_criteria>
3 reference files exist with 11 total opinions in skill-optimized format. Every opinion has Stance, Why, Do, Don't sections. Branded types and variance annotations are correctly merged into generics.md. tsconfig and conditional types are correctly merged into tsconfig-advanced.md. No framework-specific content.
</success_criteria>

<output>
After completion, create `.planning/phases/02-claude-skill/02-04-SUMMARY.md`
</output>

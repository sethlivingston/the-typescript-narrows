---
phase: 02-claude-skill
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - skill/the-typescript-narrows/references/type-safety.md
  - skill/the-typescript-narrows/references/type-declarations.md
  - skill/the-typescript-narrows/references/null-handling.md
  - skill/the-typescript-narrows/references/async-promises.md
autonomous: true
requirements: [SKIL-01, SKIL-02, SKIL-03, SKIL-04, SKIL-05, SKIL-06, SKIL-09, SKIL-10]

must_haves:
  truths:
    - "An AI agent loading type-safety.md gets complete guidance for all 7 type safety opinions"
    - "An AI agent loading type-declarations.md gets the interface-vs-type and enum stances with rationale"
    - "An AI agent loading null-handling.md gets narrowing patterns and exhaustive check guidance"
    - "An AI agent loading async-promises.md gets floating promise and async/await discipline"
    - "Every opinion includes a Why rationale the agent can relay to developers"
    - "Every opinion includes Do and Don't code examples"
  artifacts:
    - path: "skill/the-typescript-narrows/references/type-safety.md"
      provides: "7 type safety opinions in skill-optimized format"
      contains: "## Never use explicit"
    - path: "skill/the-typescript-narrows/references/type-declarations.md"
      provides: "5 type declaration opinions in skill-optimized format"
      contains: "## Use interfaces for object shapes"
    - path: "skill/the-typescript-narrows/references/null-handling.md"
      provides: "6 null handling opinions in skill-optimized format"
      contains: "## Handle all cases"
    - path: "skill/the-typescript-narrows/references/async-promises.md"
      provides: "5 async opinions in skill-optimized format"
      contains: "## Always handle Promise rejections"
  key_links:
    - from: "skill/the-typescript-narrows/SKILL.md"
      to: "skill/the-typescript-narrows/references/type-safety.md"
      via: "relative markdown link in Type Safety section"
      pattern: "references/type-safety\\.md"
---

<objective>
Author reference files for Type Safety, Type Declarations, Null Handling, and Async topics (23 opinions total).

Purpose: These reference files provide the full opinion details (stance, rationale, do/don't examples) that SKILL.md links to. Each file is independently loadable -- an agent can load just one reference file and get complete guidance for that topic.

Output: 4 reference files in `skill/the-typescript-narrows/references/`
</objective>

<execution_context>
@/Users/sethlivingston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sethlivingston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-claude-skill/02-CONTEXT.md
@.planning/phases/02-claude-skill/02-RESEARCH.md

<interfaces>
From skill/the-typescript-narrows/SKILL.md (created by plan 02-01):
- Links to references/type-safety.md, references/type-declarations.md, references/null-handling.md, references/async-promises.md
- Each topic section lists opinion stances that reference files must expand on
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author type-safety.md and type-declarations.md reference files</name>
  <files>skill/the-typescript-narrows/references/type-safety.md, skill/the-typescript-narrows/references/type-declarations.md</files>
  <read_first>
    - skill/the-typescript-narrows/SKILL.md (see which opinions are listed under Type Safety and Type Declarations)
    - docs/opinions/no-explicit-any.md (source opinion)
    - docs/opinions/prefer-unknown.md (source opinion)
    - docs/opinions/no-type-assertions.md (source opinion)
    - docs/opinions/no-non-null-assertion.md (source opinion)
    - docs/opinions/strict-boolean-expressions.md (source opinion)
    - docs/opinions/use-unknown-in-catch.md (source opinion)
    - docs/opinions/no-unsafe-return.md (source opinion)
    - docs/opinions/prefer-interface.md (source opinion)
    - docs/opinions/ban-enums.md (source opinion)
    - docs/opinions/no-namespace.md (source opinion)
    - docs/opinions/consistent-type-imports.md (source opinion)
    - docs/opinions/no-const-enum.md (source opinion)
    - .planning/phases/02-claude-skill/02-CONTEXT.md (format decisions)
  </read_first>
  <action>
Create 2 reference files using this exact format per opinion:

```markdown
## [Imperative stance title]

**Stance:** [One clear sentence from corpus Stance section, rewritten in imperative voice]

**Why:** [Tighter version of corpus Why section -- 1-3 sentences max, focus on concrete consequences]

**Do:**
```typescript
[Code example from corpus Do section, kept to 3-8 lines]
```

**Don't:**
```typescript
[Code example from corpus Don't section, kept to 3-8 lines]
```

[Optional] **Exception:** [One sentence if corpus has an Exceptions section worth preserving]

---
```

Each opinion separated by `---` horizontal rule.

**type-safety.md** -- Title: `# Type Safety`. Contains 7 opinions in this order:
1. no-explicit-any: "Never use explicit `any`"
2. prefer-unknown: "Use `unknown` for values of uncertain type"
3. no-type-assertions: "Restrict type assertions to proven-safe patterns"
4. no-non-null-assertion: "Do not use the `!` postfix operator"
5. strict-boolean-expressions: "Require explicit boolean comparisons"
6. use-unknown-in-catch: "Type catch clause variables as `unknown`"
7. no-unsafe-return: "Do not return unsafe `any`-typed values"

**type-declarations.md** -- Title: `# Type Declarations`. Contains 5 opinions in this order:
1. prefer-interface: "Use interfaces for object shapes, types for unions/intersections/utilities"
2. ban-enums: "Ban enums; use `as const` objects with type unions"
3. no-namespace: "Do not use TypeScript namespaces"
4. consistent-type-imports: "Use `import type` for type-only imports"
5. no-const-enum: "Never use `const enum`"

**Content transformation rules:**
- Rewrite in imperative voice (not "should", "is recommended")
- Tighten prose: remove hedging, qualifiers, extra explanation
- Keep code examples from corpus but trim to 3-8 lines if longer
- Include exceptions only when they are actionable and common (skip rare edge cases)
- No framework-specific content (no React, Next, Express, Angular, Vue)
- Each file starts with `# Topic Name` H1 heading
- No YAML frontmatter in reference files (only SKILL.md has frontmatter)
  </action>
  <verify>
    <automated>grep -c '^\#\# ' skill/the-typescript-narrows/references/type-safety.md | xargs -I{} test {} -eq 7 && echo "type-safety PASS: 7 opinions" || echo "type-safety FAIL: expected 7 H2 headings"; grep -c '^\#\# ' skill/the-typescript-narrows/references/type-declarations.md | xargs -I{} test {} -eq 5 && echo "type-declarations PASS: 5 opinions" || echo "type-declarations FAIL: expected 5 H2 headings"</automated>
  </verify>
  <acceptance_criteria>
    - skill/the-typescript-narrows/references/type-safety.md exists and starts with `# Type Safety`
    - type-safety.md contains exactly 7 H2 headings (## sections)
    - type-safety.md contains 7 occurrences of `**Stance:**`
    - type-safety.md contains 7 occurrences of `**Why:**`
    - type-safety.md contains at least 7 occurrences of ` ```typescript` (Do blocks)
    - type-safety.md contains `## Never use explicit` (first opinion)
    - skill/the-typescript-narrows/references/type-declarations.md exists and starts with `# Type Declarations`
    - type-declarations.md contains exactly 5 H2 headings
    - type-declarations.md contains 5 occurrences of `**Stance:**`
    - type-declarations.md contains 5 occurrences of `**Why:**`
    - type-declarations.md contains `## Use interfaces for object shapes` (prefer-interface opinion)
    - type-declarations.md contains `## Ban enums` or `## Ban enums;` (ban-enums opinion)
    - No occurrence of "react", "next.js", "express", "angular", "vue" (case-insensitive) in either file
  </acceptance_criteria>
  <done>type-safety.md has 7 opinions and type-declarations.md has 5 opinions, each with Stance, Why, Do, Don't sections in skill-optimized format.</done>
</task>

<task type="auto">
  <name>Task 2: Author null-handling.md and async-promises.md reference files</name>
  <files>skill/the-typescript-narrows/references/null-handling.md, skill/the-typescript-narrows/references/async-promises.md</files>
  <read_first>
    - skill/the-typescript-narrows/SKILL.md (see which opinions are listed under Null Handling and Async)
    - docs/opinions/exhaustive-switch.md
    - docs/opinions/prefer-optional-chaining.md
    - docs/opinions/no-unnecessary-condition.md
    - docs/opinions/prefer-nullish-coalescing.md
    - docs/opinions/use-type-narrowing.md
    - docs/opinions/strict-null-checks.md
    - docs/opinions/no-floating-promises.md
    - docs/opinions/no-misused-promises.md
    - docs/opinions/require-await.md
    - docs/opinions/return-await.md
    - docs/opinions/prefer-async-await.md
    - skill/the-typescript-narrows/references/type-safety.md (established format reference from Task 1)
  </read_first>
  <action>
Create 2 reference files using the same format established in Task 1 (H1 title, H2 per opinion with Stance/Why/Do/Don't/optional Exception, separated by `---`).

**null-handling.md** -- Title: `# Null Handling and Narrowing`. Contains 6 opinions:
1. exhaustive-switch: "Handle all cases in discriminated union switches"
2. prefer-optional-chaining: "Use optional chaining over manual null checks"
3. no-unnecessary-condition: "Do not write conditions that are always true or false"
4. prefer-nullish-coalescing: "Use `??` instead of `||` for nullish fallbacks"
5. use-type-narrowing: "Narrow types with guards instead of assertions"
6. strict-null-checks: "Enable `strictNullChecks` (never disable)"

**async-promises.md** -- Title: `# Async and Promises`. Contains 5 opinions:
1. no-floating-promises: "Always handle Promise rejections"
2. no-misused-promises: "Do not pass Promises where void is expected"
3. require-await: "Do not mark functions `async` unless they use `await`"
4. return-await: "Always `return await` in try/catch blocks"
5. prefer-async-await: "Use async/await over `.then()` chains"

Same content transformation rules as Task 1: imperative voice, tight prose, 3-8 line code examples, no framework content. Read existing opinion files for source material but rewrite for skill-optimized format.
  </action>
  <verify>
    <automated>grep -c '^\#\# ' skill/the-typescript-narrows/references/null-handling.md | xargs -I{} test {} -eq 6 && echo "null-handling PASS: 6 opinions" || echo "null-handling FAIL: expected 6 H2 headings"; grep -c '^\#\# ' skill/the-typescript-narrows/references/async-promises.md | xargs -I{} test {} -eq 5 && echo "async-promises PASS: 5 opinions" || echo "async-promises FAIL: expected 5 H2 headings"</automated>
  </verify>
  <acceptance_criteria>
    - skill/the-typescript-narrows/references/null-handling.md exists and starts with `# Null Handling`
    - null-handling.md contains exactly 6 H2 headings
    - null-handling.md contains 6 occurrences of `**Stance:**`
    - null-handling.md contains 6 occurrences of `**Why:**`
    - null-handling.md contains `## Handle all cases` (exhaustive-switch)
    - null-handling.md contains `## Enable` (strict-null-checks)
    - skill/the-typescript-narrows/references/async-promises.md exists and starts with `# Async`
    - async-promises.md contains exactly 5 H2 headings
    - async-promises.md contains 5 occurrences of `**Stance:**`
    - async-promises.md contains `## Always handle Promise rejections` (no-floating-promises)
    - async-promises.md contains `## Use async/await` (prefer-async-await)
    - No framework-specific content in either file
  </acceptance_criteria>
  <done>null-handling.md has 6 opinions and async-promises.md has 5 opinions, each with Stance, Why, Do, Don't sections in skill-optimized format.</done>
</task>

</tasks>

<verification>
- Each reference file has the correct number of H2 opinion headings
- Each opinion has **Stance:**, **Why:**, **Do:**, **Don't:** sections
- `grep -ric 'react\|next\|express\|angular\|vue' skill/the-typescript-narrows/references/type-safety.md skill/the-typescript-narrows/references/type-declarations.md skill/the-typescript-narrows/references/null-handling.md skill/the-typescript-narrows/references/async-promises.md` returns 0
- Total opinions across 4 files: 7 + 5 + 6 + 5 = 23
</verification>

<success_criteria>
4 reference files exist with 23 total opinions in skill-optimized format. Every opinion has Stance, Why, Do, Don't sections. No framework-specific content. Format is consistent across all files (imperative voice, tight prose, code examples).
</success_criteria>

<output>
After completion, create `.planning/phases/02-claude-skill/02-02-SUMMARY.md`
</output>

---
phase: 01-opinion-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - docs/opinions/prefer-const.md
  - docs/opinions/no-var.md
  - docs/opinions/prefer-readonly.md
  - docs/opinions/prefer-readonly-params.md
  - docs/opinions/no-mutable-exports.md
  - docs/opinions/named-exports-only.md
  - docs/opinions/ban-barrel-files.md
  - docs/opinions/explicit-imports.md
  - docs/opinions/no-circular-deps.md
  - docs/opinions/single-discriminant.md
  - docs/opinions/no-destructure-before-narrow.md
  - docs/opinions/exhaustive-discrimination.md
  - docs/opinions/constrain-generics.md
  - docs/opinions/no-unnecessary-generics.md
  - docs/opinions/prefer-generics-over-overloads.md
  - docs/opinions/variance-annotations.md
  - docs/opinions/branded-types.md
autonomous: true
requirements: [OPIN-02, OPIN-03]

must_haves:
  truths:
    - "17 opinion files exist covering Immutability, Module Organization, Discriminated Unions, Generics, and Branded Types topics"
    - "Every opinion file has valid YAML frontmatter with all required fields"
    - "Every opinion file has Stance, Why, Do, and Don't body sections with TypeScript code examples"
    - "Custom lint opinions (ban-barrel-files, single-discriminant, no-destructure-before-narrow) have lint type: custom"
  artifacts:
    - path: "docs/opinions/ban-barrel-files.md"
      provides: "Custom lint rule opinion for barrel file ban"
      contains: "type: custom"
    - path: "docs/opinions/branded-types.md"
      provides: "Differentiator branded types opinion"
      contains: "enforcement: skill-only"
    - path: "docs/opinions/single-discriminant.md"
      provides: "Custom lint rule opinion for discriminated unions"
      contains: "type: custom"
  key_links:
    - from: "docs/opinions/single-discriminant.md"
      to: "docs/opinions/exhaustive-discrimination.md"
      via: "related field cross-reference"
      pattern: "related:.*exhaustive-discrimination"
---

<objective>
Author 17 opinion files covering Immutability & Const (5), Module Organization (4), Discriminated Unions (3), Generics & Advanced Types (4), and Branded & Nominal Types (1).

Purpose: Creates the final batch of opinions, completing the full 50-opinion corpus. Includes the custom-lint opinions that will drive Phase 4 custom rule development.
Output: 17 markdown files in docs/opinions/
</objective>

<execution_context>
@/Users/sethlivingston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sethlivingston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-opinion-foundation/01-CONTEXT.md
@.planning/phases/01-opinion-foundation/01-RESEARCH.md
@.planning/phases/01-opinion-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Opinion file template. Every file MUST follow this exact structure. -->

```yaml
---
id: slug-based-id                   # Required. Matches filename (without .md)
title: Human-readable title          # Required. Short, imperative.
severity: bug-prevention             # Required. One of: bug-prevention, maintenance, style
enforcement: both                    # Required. One of: skill-only, lint-enforceable, both
confidence: strong                   # Required. One of: strong, moderate
tags: [type-safety, any]             # Required. Freeform array, at least one tag
related: [prefer-unknown]            # Optional. Array of other opinion IDs
lint:                                # Required if enforcement != skill-only
  type: existing                     # One of: existing, custom
  rule: "@typescript-eslint/no-explicit-any"  # Required if type: existing
---
```

Body sections (in order): `## Stance`, `## Why`, `## Do` (one or more), `## Don't` (one or more), `## Exceptions` (optional).

Code examples: 3-8 line TypeScript snippets. Multiple Do/Don't blocks allowed when pattern has distinct variations.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author Immutability & Const opinions (5) and Module Organization opinions (4)</name>
  <files>
    docs/opinions/prefer-const.md
    docs/opinions/no-var.md
    docs/opinions/prefer-readonly.md
    docs/opinions/prefer-readonly-params.md
    docs/opinions/no-mutable-exports.md
    docs/opinions/named-exports-only.md
    docs/opinions/ban-barrel-files.md
    docs/opinions/explicit-imports.md
    docs/opinions/no-circular-deps.md
  </files>
  <read_first>
    .planning/phases/01-opinion-foundation/01-RESEARCH.md
    .planning/phases/01-opinion-foundation/01-CONTEXT.md
    docs/opinions/README.md
    docs/opinions/INDEX.md
    docs/opinions/no-explicit-any.md
  </read_first>
  <action>
Create 9 opinion files. Read no-explicit-any.md first to match the established format exactly.

**Immutability & Const opinions:**

1. `prefer-const.md` -- id: prefer-const, title: "Use const by default; let only when reassigned", severity: style, enforcement: both, confidence: strong, tags: [immutability, variables], related: [no-var, no-mutable-exports], lint: {type: existing, rule: "prefer-const"}
   NOTE: `prefer-const` is an ESLint core rule (no `@typescript-eslint/` prefix).
   - Stance: Declare all variables with `const`. Use `let` only when the variable will be reassigned.
   - Why: `const` signals intent -- this value does not change. It prevents accidental reassignment and makes code easier to reason about.
   - Do: `const items = getItems();` and `let count = 0; count++;`
   - Don't: `let items = getItems();` (never reassigned)

2. `no-var.md` -- id: no-var, title: "Never use var", severity: style, enforcement: both, confidence: strong, tags: [immutability, variables], related: [prefer-const], lint: {type: existing, rule: "no-var"}
   NOTE: `no-var` is an ESLint core rule.
   - Stance: Never use `var`. Use `const` or `let`.
   - Why: `var` has function scope (not block scope) and is hoisted, creating surprising behavior. `const` and `let` have block scope and are not hoisted.
   - Do: `const name = "Alice";` or `let count = 0;`
   - Don't: `var name = "Alice";`

3. `prefer-readonly.md` -- id: prefer-readonly, title: "Mark class properties readonly when not reassigned", severity: maintenance, enforcement: both, confidence: moderate, tags: [immutability, classes], related: [prefer-const, prefer-readonly-params], lint: {type: existing, rule: "@typescript-eslint/prefer-readonly"}
   - Stance: Mark class properties `readonly` if they are not reassigned after construction.
   - Why: `readonly` documents intent and prevents accidental mutation. It also enables the compiler to detect unintended reassignment during refactoring.
   - Do: `class Service { readonly baseUrl: string; constructor(url: string) { this.baseUrl = url; } }`
   - Don't: `class Service { baseUrl: string; ... }` (never reassigned but not marked readonly)

4. `prefer-readonly-params.md` -- id: prefer-readonly-params, title: "Use Readonly<T> and ReadonlyArray<T> in function params", severity: maintenance, enforcement: both, confidence: moderate, tags: [immutability, functions, generics], related: [prefer-readonly, prefer-const], lint: {type: existing, rule: "@typescript-eslint/prefer-readonly-parameter-types"}
   - Stance: Use `Readonly<T>` for object parameters and `ReadonlyArray<T>` (or `readonly T[]`) for array parameters.
   - Why: Mutable parameters allow functions to silently modify their inputs, creating action-at-a-distance bugs. Readonly parameters make it explicit that the function does not mutate its inputs.
   - Do: `function process(items: readonly string[]): number { return items.length; }`
   - Don't: `function process(items: string[]): number { items.push("extra"); return items.length; }`
   - Exceptions: Third-party types that are not readonly-compatible may need exemption. Performance-critical code that mutates in place may use mutable params with clear documentation.

5. `no-mutable-exports.md` -- id: no-mutable-exports, title: "Do not export mutable bindings", severity: maintenance, enforcement: both, confidence: strong, tags: [immutability, modules], related: [prefer-const, named-exports-only], lint: {type: existing, rule: "import/no-mutable-exports"}
   NOTE: This rule is from the `eslint-plugin-import` package, not typescript-eslint.
   - Stance: Do not export `let` or `var` bindings. Only export `const`, `function`, and `class` declarations.
   - Why: Exported mutable bindings create shared mutable state across modules. Any importer can read a stale value if another module mutates the export. This makes behavior unpredictable and hard to debug.
   - Do: `export const config = { ... } as const;`
   - Don't: `export let currentUser: User | null = null;`

**Module Organization opinions:**

6. `named-exports-only.md` -- id: named-exports-only, title: "Use named exports; ban default exports", severity: maintenance, enforcement: both, confidence: strong, tags: [modules, imports, exports], related: [consistent-type-imports, ban-barrel-files], lint: {type: existing, rule: "import/no-default-export"}
   NOTE: This rule is from the `eslint-plugin-import` package.
   - Stance: Use named exports exclusively. Do not use default exports.
   - Why: Default exports have no canonical name -- importers can name them anything, making it impossible to grep for usages. Named exports enforce a single name, improving discoverability and refactoring.
   - Do: `export function createUser() { ... }` then `import { createUser } from "./user";`
   - Don't: `export default function() { ... }` then `import whatever from "./user";`
   - Exceptions: Frameworks that require default exports (Next.js pages, Storybook stories) at specific file paths.

7. `ban-barrel-files.md` -- id: ban-barrel-files, title: "Do not use barrel files (index.ts re-exports)", severity: maintenance, enforcement: both, confidence: moderate, tags: [modules, imports, performance], related: [named-exports-only, explicit-imports], lint: {type: custom}
   - Stance: Do not create `index.ts` files that re-export from other modules (barrel files).
   - Why: Barrel files defeat tree-shaking (importing one symbol pulls in all re-exports), create circular dependency risks, slow down IDE auto-imports, and hide the actual module structure.
   - Do: `import { User } from "./models/user";` (import from the source file)
   - Don't: `import { User } from "./models";` (imports from models/index.ts barrel)

8. `explicit-imports.md` -- id: explicit-imports, title: "Import from the source module, not through re-exports", severity: maintenance, enforcement: skill-only, confidence: moderate, tags: [modules, imports], related: [ban-barrel-files, named-exports-only]
   (No lint field -- skill-only)
   - Stance: Import directly from the module that defines the symbol. Do not import through intermediate re-export files.
   - Why: Direct imports create a clear dependency graph -- you can trace exactly where each symbol comes from. Re-export chains obscure the actual source and create unnecessary coupling between modules.
   - Do: `import { validateEmail } from "./validators/email";`
   - Don't: `import { validateEmail } from "./validators";` (re-exported through barrel)

9. `no-circular-deps.md` -- id: no-circular-deps, title: "Avoid circular dependencies", severity: bug-prevention, enforcement: both, confidence: strong, tags: [modules, architecture], related: [ban-barrel-files, explicit-imports], lint: {type: existing, rule: "import/no-cycle"}
   NOTE: This rule is from the `eslint-plugin-import` package.
   - Stance: Do not create circular dependency chains between modules.
   - Why: Circular dependencies cause partially initialized modules -- a module may import from another that has not finished executing yet, resulting in `undefined` imports at runtime. They also indicate tangled architecture.
   - Do: Extract shared types into a separate module that both sides import from
   - Don't: `a.ts` imports from `b.ts` which imports from `a.ts`
  </action>
  <verify>
    <automated>ls docs/opinions/{prefer-const,no-var,prefer-readonly,prefer-readonly-params,no-mutable-exports,named-exports-only,ban-barrel-files,explicit-imports,no-circular-deps}.md 2>/dev/null | wc -l | xargs test 9 -eq && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - 9 opinion files exist in docs/opinions/
    - prefer-const.md contains `rule: "prefer-const"` (ESLint core, no @typescript-eslint prefix)
    - no-var.md contains `rule: "no-var"` (ESLint core)
    - prefer-readonly.md contains `severity: maintenance` and `confidence: moderate`
    - prefer-readonly-params.md contains `rule: "@typescript-eslint/prefer-readonly-parameter-types"`
    - no-mutable-exports.md contains `rule: "import/no-mutable-exports"`
    - named-exports-only.md contains `rule: "import/no-default-export"`
    - ban-barrel-files.md contains `type: custom` in its lint field
    - explicit-imports.md contains `enforcement: skill-only` and does NOT contain `lint:`
    - no-circular-deps.md contains `rule: "import/no-cycle"`
    - Each file contains `## Stance`, `## Why`, `## Do`, `## Don't` headings
    - Each Do/Don't section contains a code block
  </acceptance_criteria>
  <done>9 opinion files created covering immutability (5) and module organization (4) with valid frontmatter and body sections</done>
</task>

<task type="auto">
  <name>Task 2: Author Discriminated Unions (3), Generics (4), and Branded Types (1) opinions</name>
  <files>
    docs/opinions/single-discriminant.md
    docs/opinions/no-destructure-before-narrow.md
    docs/opinions/exhaustive-discrimination.md
    docs/opinions/constrain-generics.md
    docs/opinions/no-unnecessary-generics.md
    docs/opinions/prefer-generics-over-overloads.md
    docs/opinions/variance-annotations.md
    docs/opinions/branded-types.md
  </files>
  <read_first>
    .planning/phases/01-opinion-foundation/01-RESEARCH.md
    docs/opinions/README.md
    docs/opinions/no-explicit-any.md
  </read_first>
  <action>
Create 8 opinion files. Read no-explicit-any.md to match the established format.

**Discriminated Unions opinions:**

10. `single-discriminant.md` -- id: single-discriminant, title: "Use a single type or kind field as discriminant", severity: maintenance, enforcement: both, confidence: moderate, tags: [discriminated-unions, patterns], related: [exhaustive-discrimination, no-destructure-before-narrow, exhaustive-switch], lint: {type: custom}
    - Stance: Use a single string literal field named `type` (or `kind` in AST contexts) as the discriminant for union types. Do not use multiple fields or boolean combinations.
    - Why: A single discriminant field enables TypeScript's built-in narrowing via `switch` and `if` checks. Multiple discriminant fields require complex intersection checks that the compiler cannot narrow automatically.
    - Do: `type Shape = { type: "circle"; radius: number } | { type: "rect"; width: number; height: number };`
    - Don't: `type Shape = { isCircle: boolean; radius?: number; width?: number; height?: number };`

11. `no-destructure-before-narrow.md` -- id: no-destructure-before-narrow, title: "Do not destructure discriminated unions before narrowing", severity: bug-prevention, enforcement: both, confidence: strong, tags: [discriminated-unions, narrowing], related: [single-discriminant, exhaustive-switch], lint: {type: custom}
    - Stance: Always narrow a discriminated union (via `switch` or `if` on the discriminant) before destructuring its properties.
    - Why: Destructuring before narrowing loses the type connection between the discriminant and the variant-specific fields. TypeScript cannot narrow destructured variables -- only the original object.
    - Do: `if (result.type === "success") { const { value } = result; }`
    - Don't: `const { type, ...rest } = result; if (type === "success") { rest.value; }` (rest is not narrowed)

12. `exhaustive-discrimination.md` -- id: exhaustive-discrimination, title: "Always handle all variants of a discriminated union", severity: bug-prevention, enforcement: both, confidence: strong, tags: [discriminated-unions, narrowing, exhaustiveness], related: [exhaustive-switch, single-discriminant], lint: {type: existing, rule: "@typescript-eslint/switch-exhaustiveness-check"}
    NOTE: This overlaps with exhaustive-switch (#13 in research) but covers broader patterns beyond switch statements.
    - Stance: When processing a discriminated union, handle every variant. Use `switch` with a `default: never` guard, or if/else chains that the compiler can verify are exhaustive.
    - Why: Unhandled variants are silent bugs -- the code path falls through or hits a catch-all that does the wrong thing. Adding a new variant to the union should cause compile errors everywhere it is not handled.
    - Do: switch statement with all cases plus `default: { const _exhaustive: never = action; throw new Error("Unhandled"); }`
    - Don't: switch with a `default: return null;` that silently ignores new variants

**Generics & Advanced Types opinions:**

13. `constrain-generics.md` -- id: constrain-generics, title: "Always constrain generic type parameters", severity: maintenance, enforcement: skill-only, confidence: moderate, tags: [generics, type-safety], related: [no-unnecessary-generics, prefer-generics-over-overloads]
    (No lint field -- skill-only)
    - Stance: Every generic type parameter should have an `extends` constraint. Unconstrained `<T>` should be rare and intentional.
    - Why: Unconstrained generics accept `any` type including `never`, `void`, and other edge cases. Constraints document the expected shape and catch misuse at the call site.
    - Do: `function merge<T extends object>(a: T, b: Partial<T>): T`
    - Don't: `function merge<T>(a: T, b: Partial<T>): T` (accepts primitives, which Partial does not affect)

14. `no-unnecessary-generics.md` -- id: no-unnecessary-generics, title: "Do not add type parameters used only once", severity: maintenance, enforcement: both, confidence: strong, tags: [generics, simplicity], related: [constrain-generics], lint: {type: existing, rule: "@typescript-eslint/no-unnecessary-type-parameters"}
    - Stance: Do not introduce a generic type parameter that appears only once in the function signature. If a type parameter is not used to relate two or more values, it adds complexity without benefit.
    - Why: A type parameter used once is equivalent to its constraint. `<T extends string>(x: T): void` is the same as `(x: string): void` -- the generic adds cognitive overhead without enabling anything.
    - Do: `function identity<T>(value: T): T` (T used twice -- relates input to output)
    - Don't: `function log<T extends string>(message: T): void` (T used once -- just use string)

15. `prefer-generics-over-overloads.md` -- id: prefer-generics-over-overloads, title: "Use generics instead of function overloads when possible", severity: maintenance, enforcement: skill-only, confidence: moderate, tags: [generics, functions], related: [constrain-generics, no-unnecessary-generics]
    (No lint field -- skill-only)
    - Stance: Prefer a single generic signature over multiple function overloads when the overloads differ only in type.
    - Why: Overloads create maintenance burden -- each signature must be kept in sync. Generic signatures express the relationship once. Overloads are also harder for callers to understand when there are many.
    - Do: `function pick<T, K extends keyof T>(obj: T, keys: K[]): Pick<T, K>`
    - Don't: multiple overload signatures for `pick(obj: User, keys: ("name" | "email")[]): ...` repeated for each type
    - Exceptions: Overloads are appropriate when the return type changes based on input in ways generics cannot express, or when the implementations are genuinely different.

16. `variance-annotations.md` -- id: variance-annotations, title: "Use in/out variance annotations on generic classes", severity: maintenance, enforcement: skill-only, confidence: moderate, tags: [generics, advanced-types, variance], related: [constrain-generics, conditional-type-safety]
    (No lint field -- skill-only)
    - Stance: Use `in` and `out` variance annotations on generic class and interface type parameters to make variance explicit.
    - Why: Without annotations, TypeScript infers variance from usage, which can change unexpectedly when code is modified. Explicit annotations document the intended variance and cause compile errors if the implementation violates it.
    - Do: `interface Producer<out T> { get(): T; }` and `interface Consumer<in T> { accept(value: T): void; }`
    - Don't: `interface Producer<T> { get(): T; }` (variance is inferred, may change silently)

**Branded & Nominal Types (1 opinion):**

17. `branded-types.md` -- id: branded-types, title: "Use branded types for domain IDs and value objects", severity: maintenance, enforcement: skill-only, confidence: moderate, tags: [branded-types, domain-modeling, type-safety], related: [constrain-generics]
    (No lint field -- skill-only)
    - Stance: Use branded types to prevent accidental mixing of structurally identical but semantically different values (user IDs vs order IDs, raw strings vs sanitized strings).
    - Why: TypeScript uses structural typing -- `string` is `string` regardless of what it represents. Branded types add a phantom property that makes the compiler distinguish between `UserId` and `OrderId`, catching mix-ups at compile time.
    - Do: `type UserId = string & { readonly __brand: "UserId" }; type OrderId = string & { readonly __brand: "OrderId" };` with a `function createUserId(id: string): UserId` factory.
    - Don't: `function getUser(id: string)` and `function getOrder(id: string)` (nothing prevents passing a user ID to getOrder)
  </action>
  <verify>
    <automated>ls docs/opinions/{single-discriminant,no-destructure-before-narrow,exhaustive-discrimination,constrain-generics,no-unnecessary-generics,prefer-generics-over-overloads,variance-annotations,branded-types}.md 2>/dev/null | wc -l | xargs test 8 -eq && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - 8 opinion files exist in docs/opinions/
    - single-discriminant.md contains `type: custom` in its lint field
    - no-destructure-before-narrow.md contains `type: custom` in its lint field and `severity: bug-prevention`
    - exhaustive-discrimination.md contains `rule: "@typescript-eslint/switch-exhaustiveness-check"`
    - constrain-generics.md contains `enforcement: skill-only` and does NOT contain `lint:`
    - no-unnecessary-generics.md contains `rule: "@typescript-eslint/no-unnecessary-type-parameters"`
    - prefer-generics-over-overloads.md contains `enforcement: skill-only`
    - variance-annotations.md contains `enforcement: skill-only` and `tags:` includes `variance`
    - branded-types.md contains `enforcement: skill-only` and body contains `__brand`
    - Each file contains `## Stance`, `## Why`, `## Do`, `## Don't` headings
    - Each Do/Don't section contains a code block
  </acceptance_criteria>
  <done>8 opinion files created covering discriminated unions (3), generics (4), and branded types (1) with valid frontmatter and body sections</done>
</task>

</tasks>

<verification>
- All 17 files exist in docs/opinions/
- Every file has valid YAML frontmatter with required fields
- Every file has Stance, Why, Do, Don't sections
- Severity tiers match research: 3 bug-prevention, 12 maintenance, 2 style in this batch
- Enforcement tags match: 10 both, 7 skill-only
- Custom lint opinions (ban-barrel-files, single-discriminant, no-destructure-before-narrow) have `type: custom`
- Skill-only opinions have no lint field
- `related` fields reference valid opinion IDs from INDEX.md
</verification>

<success_criteria>
- 17 opinion files exist with correct frontmatter and body structure
- Code examples are TypeScript, 3-8 lines, and demonstrate the opinion clearly
- Cross-references in `related` fields are accurate
- All 50 opinions across all plans now exist in docs/opinions/
</success_criteria>

<output>
After completion, create `.planning/phases/01-opinion-foundation/01-04-SUMMARY.md`
</output>

---
phase: 01-opinion-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - docs/opinions/no-explicit-any.md
  - docs/opinions/prefer-unknown.md
  - docs/opinions/no-type-assertions.md
  - docs/opinions/no-non-null-assertion.md
  - docs/opinions/strict-boolean-expressions.md
  - docs/opinions/use-unknown-in-catch.md
  - docs/opinions/no-unsafe-return.md
  - docs/opinions/prefer-interface.md
  - docs/opinions/ban-enums.md
  - docs/opinions/no-namespace.md
  - docs/opinions/consistent-type-imports.md
  - docs/opinions/no-const-enum.md
  - docs/opinions/naming-convention.md
  - docs/opinions/no-hungarian-notation.md
  - docs/opinions/boolean-naming.md
  - docs/opinions/strict-tsconfig.md
  - docs/opinions/no-unchecked-index.md
autonomous: true
requirements: [OPIN-02, OPIN-03]

must_haves:
  truths:
    - "17 opinion files exist covering Type Safety, Type Declarations, Naming Conventions, and tsconfig Strictness topics"
    - "Every opinion file has valid YAML frontmatter with all required fields (id, title, severity, enforcement, confidence, tags)"
    - "Every opinion file has Stance, Why, Do, and Don't body sections with TypeScript code examples"
    - "Lint-enforceable opinions include the lint field with type and rule"
  artifacts:
    - path: "docs/opinions/no-explicit-any.md"
      provides: "Template-setting first opinion"
      contains: "severity: bug-prevention"
    - path: "docs/opinions/ban-enums.md"
      provides: "Custom lint rule opinion"
      contains: "type: custom"
    - path: "docs/opinions/strict-tsconfig.md"
      provides: "Skill-only tsconfig opinion"
      contains: "enforcement: skill-only"
  key_links:
    - from: "docs/opinions/no-explicit-any.md"
      to: "docs/opinions/prefer-unknown.md"
      via: "related field cross-reference"
      pattern: "related:.*prefer-unknown"
---

<objective>
Author 17 opinion files covering Type Safety (7), Type Declarations (5), Naming Conventions (3), and tsconfig Strictness (2).

Purpose: Creates the first batch of opinions, including the highest-severity type safety opinions that form the backbone of the corpus.
Output: 17 markdown files in docs/opinions/
</objective>

<execution_context>
@/Users/sethlivingston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sethlivingston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-opinion-foundation/01-CONTEXT.md
@.planning/phases/01-opinion-foundation/01-RESEARCH.md
@.planning/phases/01-opinion-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Opinion file template from RESEARCH.md. Every file MUST follow this exact structure. -->

```yaml
---
id: slug-based-id                   # Required. Matches filename (without .md)
title: Human-readable title          # Required. Short, imperative.
severity: bug-prevention             # Required. One of: bug-prevention, maintenance, style
enforcement: both                    # Required. One of: skill-only, lint-enforceable, both
confidence: strong                   # Required. One of: strong, moderate
tags: [type-safety, any]             # Required. Freeform array, at least one tag
related: [prefer-unknown]            # Optional. Array of other opinion IDs
lint:                                # Required if enforcement != skill-only
  type: existing                     # One of: existing, custom
  rule: "@typescript-eslint/no-explicit-any"  # Required if type: existing
---
```

Body sections (in order): `## Stance`, `## Why`, `## Do` (one or more), `## Don't` (one or more), `## Exceptions` (optional).

Code examples: 3-8 line TypeScript snippets. Multiple Do/Don't blocks allowed when pattern has distinct variations.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author Type Safety opinions (7) and Type Declarations opinions (5)</name>
  <files>
    docs/opinions/no-explicit-any.md
    docs/opinions/prefer-unknown.md
    docs/opinions/no-type-assertions.md
    docs/opinions/no-non-null-assertion.md
    docs/opinions/strict-boolean-expressions.md
    docs/opinions/use-unknown-in-catch.md
    docs/opinions/no-unsafe-return.md
    docs/opinions/prefer-interface.md
    docs/opinions/ban-enums.md
    docs/opinions/no-namespace.md
    docs/opinions/consistent-type-imports.md
    docs/opinions/no-const-enum.md
  </files>
  <read_first>
    .planning/phases/01-opinion-foundation/01-RESEARCH.md
    .planning/phases/01-opinion-foundation/01-CONTEXT.md
    docs/opinions/README.md
    docs/opinions/INDEX.md
  </read_first>
  <action>
Create 12 opinion files. Each file must follow the exact template from the interfaces section above. Use the research enumeration for metadata (severity, enforcement, confidence, lint mapping). Author original Stance, Why, Do, Don't, and optional Exceptions sections.

**Type Safety opinions (all severity: bug-prevention, all enforcement: both, all confidence: strong except #5):**

1. `no-explicit-any.md` -- id: no-explicit-any, title: "Never use explicit any", tags: [type-safety, any], related: [prefer-unknown, no-unsafe-return], lint: {type: existing, rule: "@typescript-eslint/no-explicit-any"}
   - Stance: Never use `any` as a type annotation. Use `unknown` instead and narrow.
   - Why: `any` disables type checking, allowing silent runtime errors.
   - Do: function accepting `unknown` with typeof narrowing
   - Don't: function accepting `any` with no safety

2. `prefer-unknown.md` -- id: prefer-unknown, title: "Use unknown for values of uncertain type", tags: [type-safety, unknown], related: [no-explicit-any, use-type-narrowing], lint: {type: existing, rule: "@typescript-eslint/no-unsafe-assignment"}
   - Stance: Default to `unknown` for values whose type is not known at compile time.
   - Why: `unknown` forces narrowing before use, catching type errors at compile time.
   - Do: parsing JSON with `unknown` then narrowing
   - Don't: parsing JSON with `any` then using directly

3. `no-type-assertions.md` -- id: no-type-assertions, title: "Restrict type assertions to proven-safe patterns", tags: [type-safety, assertions], related: [no-explicit-any, use-type-narrowing], lint: {type: existing, rule: "@typescript-eslint/consistent-type-assertions"}
   - Stance: Do not use `as` type assertions unless narrowing is impossible and the assertion is provably safe.
   - Why: Type assertions bypass the compiler -- if the assertion is wrong, you get a runtime error with no warning.
   - Do: type guard function that narrows safely
   - Don't: `as` assertion on unvalidated data
   - Exceptions: Test fixtures, interop with untyped APIs where validation happens externally.

4. `no-non-null-assertion.md` -- id: no-non-null-assertion, title: "Do not use the ! postfix operator", tags: [type-safety, null-handling], related: [prefer-optional-chaining, prefer-nullish-coalescing], lint: {type: existing, rule: "@typescript-eslint/no-non-null-assertion"}
   - Stance: Never use the `!` non-null assertion operator.
   - Why: `!` tells the compiler to trust you that a value is not null/undefined. If you are wrong, it crashes at runtime.
   - Do: if-check or optional chaining
   - Don't: `element!.textContent`

5. `strict-boolean-expressions.md` -- id: strict-boolean-expressions, title: "Require explicit boolean comparisons", confidence: moderate, tags: [type-safety, conditionals], related: [no-unnecessary-condition], lint: {type: existing, rule: "@typescript-eslint/strict-boolean-expressions"}
   - Stance: Use explicit comparisons (`=== null`, `!== undefined`, `.length > 0`) instead of truthy/falsy checks.
   - Why: Truthy/falsy checks silently coerce `0`, `""`, and `NaN` to false, causing subtle bugs.
   - Do: `if (items.length > 0)` and `if (name !== "")`
   - Don't: `if (items.length)` and `if (name)`
   - Exceptions: Boolean variables themselves (`if (isReady)`) are fine.

6. `use-unknown-in-catch.md` -- id: use-unknown-in-catch, title: "Type catch clause variables as unknown", tags: [type-safety, error-handling], related: [typed-errors, no-explicit-any], lint: {type: existing, rule: "@typescript-eslint/use-unknown-in-catch-callback-variable"}
   - Stance: Treat catch clause variables as `unknown` and narrow before accessing properties.
   - Why: Anything can be thrown in JavaScript. Assuming `error` is an `Error` instance will crash if someone throws a string or object.
   - Do: `catch (error) { if (error instanceof Error) { ... } }`
   - Don't: `catch (error) { console.log(error.message); }` (assumes Error)

7. `no-unsafe-return.md` -- id: no-unsafe-return, title: "Do not return unsafe any-typed values", tags: [type-safety, any], related: [no-explicit-any, prefer-unknown], lint: {type: existing, rule: "@typescript-eslint/no-unsafe-return"}
   - Stance: Never return a value typed as `any` from a function with a typed return signature.
   - Why: An `any` return silently poisons the calling code -- the caller believes they have a typed value but actually have unchecked data.
   - Do: validate and narrow the value before returning
   - Don't: `return JSON.parse(data)` where return type is a specific interface

**Type Declarations opinions:**

8. `prefer-interface.md` -- id: prefer-interface, title: "Use interfaces for object shapes, types for unions/intersections/utilities", severity: maintenance, enforcement: both, confidence: moderate, tags: [type-declarations, interface, type-alias], related: [no-namespace], lint: {type: existing, rule: "@typescript-eslint/consistent-type-definitions"}
   - Stance: Use `interface` for object shapes (data contracts, props, configs). Use `type` for unions, intersections, mapped types, and utility types.
   - Why: Interfaces are extendable and produce better error messages. Types are required for unions and intersections. Consistent usage eliminates the daily "type or interface?" decision.
   - Do: `interface User { name: string }` and `type Result = Success | Failure`
   - Don't: `type User = { name: string }` for a plain object shape

9. `ban-enums.md` -- id: ban-enums, title: "Ban enums; use as const objects with type unions", severity: maintenance, enforcement: both, confidence: strong, tags: [type-declarations, enums], related: [no-const-enum], lint: {type: custom}
   - Stance: Do not use TypeScript enums. Use `as const` objects with derived type unions instead.
   - Why: Enums generate runtime code, have surprising behaviors (reverse mappings, const enum inlining issues in declaration files), and cannot be tree-shaken. `as const` objects are plain JavaScript with full type safety.
   - Do: `const Status = { Active: "active", Inactive: "inactive" } as const; type Status = typeof Status[keyof typeof Status];`
   - Don't: `enum Status { Active = "active", Inactive = "inactive" }`

10. `no-namespace.md` -- id: no-namespace, title: "Do not use TypeScript namespaces", severity: maintenance, enforcement: both, confidence: strong, tags: [type-declarations, modules], related: [named-exports-only], lint: {type: existing, rule: "@typescript-eslint/no-namespace"}
    - Stance: Never use `namespace`. Use ES modules for code organization.
    - Why: Namespaces are a legacy TypeScript feature that predates ES modules. They create unnecessary indirection and do not work with tree-shaking.
    - Do: ES module with named exports
    - Don't: `namespace Utils { export function ... }`

11. `consistent-type-imports.md` -- id: consistent-type-imports, title: "Use import type for type-only imports", severity: style, enforcement: both, confidence: strong, tags: [type-declarations, imports], related: [named-exports-only], lint: {type: existing, rule: "@typescript-eslint/consistent-type-imports"}
    - Stance: Use `import type` for imports that are only used as types and are erased at compile time.
    - Why: `import type` makes the compile-time vs runtime distinction explicit, enables better tree-shaking, and prevents accidental side effects from type-only imports.
    - Do: `import type { User } from "./types";`
    - Don't: `import { User } from "./types";` (when User is only used as a type)

12. `no-const-enum.md` -- id: no-const-enum, title: "Never use const enum", severity: bug-prevention, enforcement: both, confidence: strong, tags: [type-declarations, enums], related: [ban-enums], lint: {type: existing, rule: "@typescript-eslint/no-unnecessary-type-parameters"}
    NOTE on lint: The correct approach is to use `no-restricted-syntax` to ban `const enum` declarations, or handle via the custom `ban-enums` rule. Set lint to: {type: custom} since it is part of the broader enum ban.
    CORRECTION: Set lint field as: `lint: {type: custom}` -- const enum ban is enforced as part of the broader ban-enums custom rule.
    - Stance: Never use `const enum`. Use `as const` objects instead.
    - Why: `const enum` values are inlined at compile time, which breaks when declaration files are consumed by other packages. The values disappear from the bundle, causing runtime errors in consumers.
    - Do: `const Direction = { Up: 0, Down: 1 } as const;`
    - Don't: `const enum Direction { Up, Down }`
  </action>
  <verify>
    <automated>ls docs/opinions/{no-explicit-any,prefer-unknown,no-type-assertions,no-non-null-assertion,strict-boolean-expressions,use-unknown-in-catch,no-unsafe-return,prefer-interface,ban-enums,no-namespace,consistent-type-imports,no-const-enum}.md 2>/dev/null | wc -l | xargs test 12 -eq && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - 12 opinion files exist in docs/opinions/
    - Each file contains YAML frontmatter delimited by `---` lines
    - Each file's frontmatter contains `id:`, `title:`, `severity:`, `enforcement:`, `confidence:`, `tags:`
    - no-explicit-any.md contains `severity: bug-prevention` and `enforcement: both`
    - ban-enums.md contains `type: custom` in its lint field
    - consistent-type-imports.md contains `severity: style`
    - no-const-enum.md contains `type: custom` in its lint field
    - prefer-interface.md contains `confidence: moderate`
    - Each file contains `## Stance`, `## Why`, `## Do`, `## Don't` headings
    - Each Do/Don't section contains a TypeScript code block (``` delimiters present)
    - no-explicit-any.md frontmatter contains `related:` with `prefer-unknown`
  </acceptance_criteria>
  <done>12 opinion files created with valid frontmatter and all required body sections including TypeScript code examples</done>
</task>

<task type="auto">
  <name>Task 2: Author Naming Conventions opinions (3) and tsconfig Strictness opinions (2)</name>
  <files>
    docs/opinions/naming-convention.md
    docs/opinions/no-hungarian-notation.md
    docs/opinions/boolean-naming.md
    docs/opinions/strict-tsconfig.md
    docs/opinions/no-unchecked-index.md
  </files>
  <read_first>
    .planning/phases/01-opinion-foundation/01-RESEARCH.md
    docs/opinions/README.md
    docs/opinions/no-explicit-any.md
  </read_first>
  <action>
Create 5 opinion files following the same template as Task 1. Read no-explicit-any.md first to match the established format exactly.

**Naming Conventions opinions (all severity: style):**

13. `naming-convention.md` -- id: naming-convention, title: "PascalCase types, camelCase values, UPPER_CASE constants", severity: style, enforcement: both, confidence: strong, tags: [naming, conventions], related: [no-hungarian-notation], lint: {type: existing, rule: "@typescript-eslint/naming-convention"}
    - Stance: Use PascalCase for types, interfaces, classes, and enums. Use camelCase for variables, functions, methods, and parameters. Use UPPER_CASE for module-level constants.
    - Why: Consistent casing makes it immediately clear whether a symbol is a type or a value. This is the dominant convention in the TypeScript ecosystem.
    - Do: `interface UserProfile`, `const maxRetries`, `const MAX_TIMEOUT = 5000`
    - Don't: `interface userProfile`, `const MaxRetries`, `const max_timeout`

14. `no-hungarian-notation.md` -- id: no-hungarian-notation, title: "Do not prefix type names (no IUser, TConfig, EStatus)", severity: style, enforcement: both, confidence: strong, tags: [naming, conventions], related: [naming-convention], lint: {type: existing, rule: "@typescript-eslint/naming-convention"}
    - Stance: Do not prefix interfaces with `I`, types with `T`, or enums with `E`.
    - Why: Hungarian notation is a C#/Java convention that adds noise in TypeScript. The language's structural type system makes prefixes redundant -- the usage context already tells you whether something is a type.
    - Do: `interface User`, `type Config`
    - Don't: `interface IUser`, `type TConfig`

15. `boolean-naming.md` -- id: boolean-naming, title: "Prefix booleans with is, has, should, can", severity: style, enforcement: skill-only, confidence: moderate, tags: [naming, conventions, readability], related: [naming-convention]
    (No lint field -- skill-only enforcement)
    - Stance: Name boolean variables and parameters with a verb prefix: `is`, `has`, `should`, `can`, `will`, `did`.
    - Why: Boolean names without prefixes are ambiguous. `disabled` could be a verb (action) or adjective (state). `isDisabled` is unambiguously a boolean state.
    - Do: `isVisible`, `hasPermission`, `shouldRetry`, `canEdit`
    - Don't: `visible`, `permission`, `retry`, `edit`

**tsconfig Strictness opinions (both severity: bug-prevention, both enforcement: skill-only):**

16. `strict-tsconfig.md` -- id: strict-tsconfig, title: "Enable strict: true and all strict family flags", severity: bug-prevention, enforcement: skill-only, confidence: strong, tags: [tsconfig, strictness, configuration], related: [no-unchecked-index, strict-null-checks]
    (No lint field -- skill-only, not lintable)
    - Stance: Always set `strict: true` in tsconfig.json. Additionally enable `exactOptionalPropertyTypes`. Never disable individual strict flags.
    - Why: `strict: true` enables `strictNullChecks`, `strictFunctionTypes`, `strictBindCallApply`, `strictPropertyInitialization`, `noImplicitAny`, `noImplicitThis`, `alwaysStrict`, and `useUnknownInCatchVariables`. Disabling any one of these opens a class of bugs the compiler would otherwise catch.
    - Do: tsconfig with `"strict": true` and `"exactOptionalPropertyTypes": true`
    - Don't: tsconfig with `"strict": true` then `"strictNullChecks": false`
    - Exceptions: Legacy migration may disable flags temporarily, but the goal is always full strict mode.

17. `no-unchecked-index.md` -- id: no-unchecked-index, title: "Enable noUncheckedIndexedAccess", severity: bug-prevention, enforcement: skill-only, confidence: strong, tags: [tsconfig, strictness, configuration], related: [strict-tsconfig]
    (No lint field -- skill-only, not lintable)
    - Stance: Enable `noUncheckedIndexedAccess` in tsconfig.json.
    - Why: Without this flag, `array[0]` returns `T` instead of `T | undefined`, and `record["key"]` returns `V` instead of `V | undefined`. This hides potential undefined access that causes runtime crashes.
    - Do: tsconfig with `"noUncheckedIndexedAccess": true`, then `const first = items[0]; if (first !== undefined) { ... }`
    - Don't: `const first = items[0]; first.toUpperCase();` (crashes if array is empty)
  </action>
  <verify>
    <automated>ls docs/opinions/{naming-convention,no-hungarian-notation,boolean-naming,strict-tsconfig,no-unchecked-index}.md 2>/dev/null | wc -l | xargs test 5 -eq && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - 5 opinion files exist in docs/opinions/
    - naming-convention.md contains `severity: style` and `enforcement: both`
    - no-hungarian-notation.md contains `severity: style` and `enforcement: both`
    - boolean-naming.md contains `enforcement: skill-only` and does NOT contain a `lint:` field
    - strict-tsconfig.md contains `enforcement: skill-only` and `severity: bug-prevention`
    - no-unchecked-index.md contains `enforcement: skill-only` and `severity: bug-prevention`
    - strict-tsconfig.md contains `exactOptionalPropertyTypes` in its body
    - no-unchecked-index.md contains `noUncheckedIndexedAccess` in its body
    - Each file contains `## Stance`, `## Why`, `## Do`, `## Don't` headings
    - Each Do/Don't section contains a code block
  </acceptance_criteria>
  <done>5 opinion files created covering naming conventions (3) and tsconfig strictness (2) with valid frontmatter and body sections</done>
</task>

</tasks>

<verification>
- All 17 files exist in docs/opinions/
- Every file has valid YAML frontmatter with required fields
- Every file has Stance, Why, Do, Don't sections
- Severity tiers match research: 8 bug-prevention, 4 maintenance, 5 style in this batch
- Enforcement tags match: 14 both, 3 skill-only
- Skill-only opinions have no lint field
- Both/lint-enforceable opinions have lint field with type and rule (for existing) or type (for custom)
- `related` fields reference valid opinion IDs from the INDEX.md master list
</verification>

<success_criteria>
- 17 opinion files exist with correct frontmatter and body structure
- Code examples are TypeScript, 3-8 lines, and demonstrate the opinion clearly
- Cross-references in `related` fields are accurate
</success_criteria>

<output>
After completion, create `.planning/phases/01-opinion-foundation/01-02-SUMMARY.md`
</output>
